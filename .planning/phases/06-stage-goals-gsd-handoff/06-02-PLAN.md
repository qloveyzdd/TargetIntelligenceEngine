---
phase: 06-stage-goals-gsd-handoff
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/features/stage-goals/build-stage-goal-handoff.ts
  - src/features/stage-goals/build-stage-goal-handoff.test.ts
  - src/app/api/runs/[runId]/handoff/route.ts
  - src/app/api/runs/[runId]/handoff/route.test.ts
  - src/components/workspace/stage-goals-panel.tsx
  - src/components/workspace/stage-goals-panel.test.tsx
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/run-shell.test.tsx
  - src/components/workspace/analysis-placeholders.tsx
  - tests/e2e/stage-goals-handoff-workflow.spec.ts
autonomous: true
requirements: [STAG-01, STAG-02, STAG-03]
user_setup: []
must_haves:
  truths:
    - "handoff 首版只输出结构化 `stageGoals` 和每阶段 focus，不直接膨胀成完整 GSD 计划。"
    - "工作台里的 Stage Goals 区域必须能生成、查看、导出 handoff，并在 reopen 后回显。"
    - "导出结构来自 persisted `run.stageGoals`，不是前端临时拼装。"
  artifacts:
    - path: "src/features/stage-goals/build-stage-goal-handoff.ts"
      provides: "稳定的 GSD handoff formatter"
      contains: "buildStageGoalHandoff"
    - path: "src/components/workspace/stage-goals-panel.tsx"
      provides: "阶段目标生成与导出面板"
      contains: "StageGoalsPanel"
    - path: "src/app/api/runs/[runId]/handoff/route.ts"
      provides: "结构化 handoff 导出接口"
      contains: "GET"
  key_links:
    - from: "src/components/workspace/run-shell.tsx"
      to: "src/components/workspace/stage-goals-panel.tsx"
      via: "在可视化层之后挂载阶段目标面板"
      pattern: "VisualIntelligenceSurface|StageGoalsPanel"
    - from: "src/app/api/runs/[runId]/handoff/route.ts"
      to: "src/features/stage-goals/build-stage-goal-handoff.ts"
      via: "route 只返回 formatter 的稳定结果"
      pattern: "buildStageGoalHandoff"
---

<objective>
把已生成的 `run.stageGoals` 接进工作台，并导出结构化 GSD handoff，让 Phase 6 真正闭环到“可继续规划”的结果层。
Purpose: 让阶段目标不再只是 persisted JSON，而是用户能看到、能复制、能 reopen、能直接喂给后续 GSD 的结构化输出。
Output: `buildStageGoalHandoff()`、`GET /api/runs/[runId]/handoff`、`StageGoalsPanel`、focused E2E。
</objective>

<execution_context>
@C:/Users/84934/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/84934/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-stage-goals-gsd-handoff/06-CONTEXT.md
@.planning/phases/06-stage-goals-gsd-handoff/06-RESEARCH.md
@.planning/phases/06-stage-goals-gsd-handoff/06-VALIDATION.md
@.planning/phases/06-stage-goals-gsd-handoff/06-01-PLAN.md
@src/components/workspace/run-shell.tsx
@src/components/workspace/analysis-placeholders.tsx
@src/components/workspace/visual-intelligence-surface.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现结构化 handoff formatter 与导出 route</name>
  <files>src/features/stage-goals/build-stage-goal-handoff.ts, src/features/stage-goals/build-stage-goal-handoff.test.ts, src/app/api/runs/[runId]/handoff/route.ts, src/app/api/runs/[runId]/handoff/route.test.ts</files>
  <read_first>
    - src/features/analysis-run/types.ts
    - src/app/api/runs/[runId]/route.ts
    - src/app/api/runs/[runId]/stage-goals/route.ts
    - .planning/phases/06-stage-goals-gsd-handoff/06-CONTEXT.md
    - .planning/phases/06-stage-goals-gsd-handoff/06-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/features/stage-goals/build-stage-goal-handoff.ts` exports `buildStageGoalHandoff`
    - handoff result contains `goalSummary`, `stageGoals`, `stageFocuses`, and `generatedAt`
    - `src/app/api/runs/[runId]/handoff/route.ts` returns 400 or 404 when required persisted data is missing
    - successful GET returns only structured handoff payload, not the whole run
    - `npm run test:unit -- --run src/features/stage-goals/build-stage-goal-handoff.test.ts src/app/api/runs/[runId]/handoff/route.test.ts` exits 0
  </acceptance_criteria>
  <action>新增 `src/features/stage-goals/build-stage-goal-handoff.ts`，把 persisted `run.stageGoals` 格式化成首版 GSD handoff payload。输出至少包含：`goalSummary`（目标名、类别、当前阶段摘要）、`stageGoals`（三阶段完整结构）、`stageFocuses`（每阶段一条简短 focus）、`generatedAt`。随后新增 `src/app/api/runs/[runId]/handoff/route.ts`，只提供结构化导出：当 run 不存在、`run.scoring` 缺失或 `run.stageGoals` 为空时返回明确错误；成功时只返回 formatter 产物，不返回整个 run。不要在这里生成 Markdown 文件，也不要扩展成完整 GSD plan 草案。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/stage-goals/build-stage-goal-handoff.test.ts src/app/api/runs/[runId]/handoff/route.test.ts</automated>
  </verify>
  <done>Phase 6 已经有稳定的 handoff payload，可供 UI 展示和后续流程消费。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在工作台接入 StageGoalsPanel 并补 focused E2E</name>
  <files>src/components/workspace/stage-goals-panel.tsx, src/components/workspace/stage-goals-panel.test.tsx, src/components/workspace/run-shell.tsx, src/components/workspace/run-shell.test.tsx, src/components/workspace/analysis-placeholders.tsx, tests/e2e/stage-goals-handoff-workflow.spec.ts</files>
  <read_first>
    - src/components/workspace/run-shell.tsx
    - src/components/workspace/analysis-placeholders.tsx
    - src/components/workspace/visual-intelligence-surface.tsx
    - src/components/workspace/scoring-panel.tsx
    - .planning/phases/06-stage-goals-gsd-handoff/06-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/components/workspace/stage-goals-panel.tsx` contains a generate action and a handoff export action
    - `src/components/workspace/run-shell.tsx` mounts `StageGoalsPanel` after the visual surface when scoring exists
    - `src/components/workspace/analysis-placeholders.tsx` no longer shows `Stage Goals` as a permanent placeholder once the real panel is mounted
    - `tests/e2e/stage-goals-handoff-workflow.spec.ts` covers generate -> export -> reopen
    - `npm run test:unit -- --run src/components/workspace/stage-goals-panel.test.tsx src/components/workspace/run-shell.test.tsx` exits 0
    - `npm run test:e2e -- tests/e2e/stage-goals-handoff-workflow.spec.ts` exits 0
  </acceptance_criteria>
  <action>新增 `src/components/workspace/stage-goals-panel.tsx`，保持当前单页工作台节奏：当 `run.scoring` 存在时显示阶段目标区域；当 `run.stageGoals` 为空时提供“Generate stage goals”动作，调用 `POST /api/runs/[runId]/stage-goals`；当 `run.stageGoals` 已存在时显示三阶段目标列表，并提供 handoff 预览/复制动作，导出内容来自 `GET /api/runs/[runId]/handoff`。随后修改 `src/components/workspace/run-shell.tsx`，把 `StageGoalsPanel` 挂在 `VisualIntelligenceSurface` 之后；再调整 `src/components/workspace/analysis-placeholders.tsx`，移除或降级原来的 `Stage Goals` 占位逻辑，避免真实面板和 placeholder 同时出现。最后新增 `tests/e2e/stage-goals-handoff-workflow.spec.ts`，覆盖“已有 scoring -> 生成阶段目标 -> 查看 handoff -> reopen `/runs/[runId]` 后仍回显 persisted `stageGoals`”主链。</action>
  <verify>
    <automated>npm run test:unit -- --run src/components/workspace/stage-goals-panel.test.tsx src/components/workspace/run-shell.test.tsx && npm run test:e2e -- tests/e2e/stage-goals-handoff-workflow.spec.ts</automated>
  </verify>
  <done>用户已经可以在现有工作台里生成阶段目标、导出 handoff，并在 reopen 后继续消费同一份 persisted 结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run.stageGoals` -> handoff payload | 导出给后续 GSD 的内容必须忠实反映 persisted 阶段目标 |
| browser panel -> export flow | UI 只能展示和复制 formatter 结果，不能临时拼接出另一套 handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | handoff route | mitigate | route 只返回结构化 handoff，不泄露整个 run 或无关 evidence 文本 |
| T-06-04 | Repudiation | StageGoalsPanel reopen flow | mitigate | focused E2E 断言 reopen 后展示的是 persisted `run.stageGoals`，不是前端临时状态 |
</threat_model>

<verification>
- [ ] `npm run test:unit -- --run src/features/stage-goals/build-stage-goal-handoff.test.ts src/components/workspace/stage-goals-panel.test.tsx src/components/workspace/run-shell.test.tsx` passes
- [ ] `npm run test:e2e -- tests/e2e/stage-goals-handoff-workflow.spec.ts` passes
- [ ] 工作台能在 scoring 后看到真实 Stage Goals 面板
- [ ] handoff 导出只消费 persisted `run.stageGoals`
- [ ] reopen `/runs/[runId]` 后三阶段目标仍可回显
</verification>

<success_criteria>
- STAG-03 的结构化导出已经落地，不依赖手工复制原始 run JSON
- Stage Goals 在工作台里闭环为“生成 -> 查看 -> 导出 -> reopen”
- Phase 6 仍保持 KISS，没有演变成完整 planner 或重型导出系统
</success_criteria>

<output>
After completion, create `.planning/phases/06-stage-goals-gsd-handoff/06-02-SUMMARY.md`
</output>
