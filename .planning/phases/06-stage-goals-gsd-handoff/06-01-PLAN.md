---
phase: 06-stage-goals-gsd-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.ts
  - src/features/analysis-run/repository.test.ts
  - src/features/stage-goals/build-stage-goals.ts
  - src/features/stage-goals/build-stage-goals.test.ts
  - src/app/api/runs/[runId]/stage-goals/route.ts
  - src/app/api/runs/[runId]/stage-goals/route.test.ts
autonomous: true
requirements: [STAG-01, STAG-02]
user_setup: []
must_haves:
  truths:
    - "阶段目标固定为 `validation`、`mvp`、`differentiation` 三段，不按 run 动态增减。"
    - "`run.stageGoals` 的每一项都必须带 `basedOnGaps`、`relatedDimensions`、`referenceProducts`、`successMetrics`、`deliverables`、`risks`。"
    - "生成阶段目标时只消费 persisted `run.scoring.gaps`，不重新计算 scoring。"
    - "一旦 `goal / dimensions / searchPlan / candidates / evidence / scoring` 变化，旧 `stageGoals` 必须失效。"
  artifacts:
    - path: "src/features/stage-goals/build-stage-goals.ts"
      provides: "gap -> stageGoals 的纯函数 builder"
      contains: "buildStageGoals"
    - path: "src/app/api/runs/[runId]/stage-goals/route.ts"
      provides: "阶段目标生成与持久化入口"
      contains: "POST"
    - path: "src/features/analysis-run/repository.ts"
      provides: "stageGoals invalidation 边界"
      contains: "stageGoals"
  key_links:
    - from: "src/app/api/runs/[runId]/scoring/route.ts"
      to: "src/app/api/runs/[runId]/stage-goals/route.ts"
      via: "只有 persisted scoring 已存在时才能继续生成阶段目标"
      pattern: "run.scoring|evidence_ready"
    - from: "src/features/analysis-run/repository.ts"
      to: "src/features/stage-goals/build-stage-goals.ts"
      via: "stage goals 依赖 scoring，因此 repository 必须清理陈旧结果"
      pattern: "scoring|stageGoals"
---

<objective>
冻结 Phase 6 的阶段目标数据契约、合成规则和持久化入口，让 `run.stageGoals` 成为从 `gap priorities` 派生出来的稳定结构化结果。
Purpose: 先把“如何从 gap 变成三段阶段目标”做成纯函数和持久化链，再让后续 UI/handoff 只消费这层稳定结果。
Output: 扩展后的 `StageGoal` 契约、统一 invalidation、`buildStageGoals()` 和 `POST /api/runs/[runId]/stage-goals`。
</objective>

<execution_context>
@C:/Users/84934/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/84934/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-stage-goals-gsd-handoff/06-CONTEXT.md
@.planning/phases/06-stage-goals-gsd-handoff/06-RESEARCH.md
@.planning/phases/06-stage-goals-gsd-handoff/06-VALIDATION.md
@src/features/analysis-run/types.ts
@src/features/analysis-run/repository.ts
@src/features/analysis-run/run-mappers.ts
@src/app/api/runs/[runId]/scoring/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩 StageGoal 契约并补统一 invalidation</name>
  <files>src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.ts, src/features/analysis-run/repository.test.ts</files>
  <read_first>
    - src/features/analysis-run/types.ts
    - src/features/analysis-run/run-mappers.ts
    - src/features/analysis-run/repository.ts
    - src/features/analysis-run/repository.test.ts
    - .planning/phases/06-stage-goals-gsd-handoff/06-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `src/features/analysis-run/types.ts` contains `basedOnGaps` and `referenceProducts`
    - `src/features/analysis-run/run-mappers.ts` accepts the expanded `StageGoal` shape
    - `src/features/analysis-run/repository.ts` clears `stageGoals` when `scoring` or any scoring upstream dependency changes
    - `npm run test:unit -- --run src/features/analysis-run/repository.test.ts` exits 0
  </acceptance_criteria>
  <action>先在 `src/features/analysis-run/types.ts` 扩展 `StageGoal`，把当前过薄的结构升级为：`stage`、`objective`、`basedOnGaps`、`relatedDimensions`、`referenceProducts`、`successMetrics`、`deliverables`、`risks`。同步修改 `src/features/analysis-run/run-mappers.ts` 的 `toStageGoals()`，让持久化 roundtrip 接受新字段并保持缺失字段时返回空数组而不是半合法对象。然后回到 `src/features/analysis-run/repository.ts`，把 `stageGoals` 纳入统一 invalidation：当 `goal / dimensions / searchPlan / candidates / evidence / scoring` 任一发生变化且这次 update 不是显式写入新 `stageGoals` 时，自动把旧 `stageGoals` 清成空数组，避免 stale handoff。最后在 `src/features/analysis-run/repository.test.ts` 增加断言，证明 scoring 更新或上游变化后旧 `stageGoals` 会被清掉。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/analysis-run/repository.test.ts</automated>
  </verify>
  <done>`StageGoal` 契约和 invalidation 规则已经稳定，后续 route/UI 可以安全消费 `run.stageGoals`。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 gap 驱动的三阶段目标生成与持久化 route</name>
  <files>src/features/stage-goals/build-stage-goals.ts, src/features/stage-goals/build-stage-goals.test.ts, src/app/api/runs/[runId]/stage-goals/route.ts, src/app/api/runs/[runId]/stage-goals/route.test.ts</files>
  <read_first>
    - src/features/scoring/build-gap-priorities.ts
    - src/features/analysis-run/types.ts
    - src/app/api/runs/[runId]/scoring/route.ts
    - .planning/phases/06-stage-goals-gsd-handoff/06-RESEARCH.md
    - .planning/phases/06-stage-goals-gsd-handoff/06-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `src/features/stage-goals/build-stage-goals.ts` exports `buildStageGoals`
    - `buildStageGoals()` always returns exactly 3 items with `stage` values `validation`, `mvp`, `differentiation`
    - each returned stage goal contains non-empty `basedOnGaps`, `relatedDimensions`, and `referenceProducts` when known gaps exist
    - `src/app/api/runs/[runId]/stage-goals/route.ts` rejects requests when `run.scoring` is missing
    - successful POST persists `run.stageGoals`
    - `npm run test:unit -- --run src/features/stage-goals/build-stage-goals.test.ts src/app/api/runs/[runId]/stage-goals/route.test.ts` exits 0
  </acceptance_criteria>
  <action>新增 `src/features/stage-goals/build-stage-goals.ts`，把 Phase 6 的固定三阶段做成纯 builder。输入至少消费 `goal`、启用的 `dimensions`、`candidates` 和 persisted `scoring.gaps`。规则要写死：先按 `gap.priority` 从高到低排序；偏验证/风险/可行性的高优先级 gap 先归入 `validation`；核心能力补齐归入 `mvp`；剩余高价值增强项归入 `differentiation`。每条输出都要填上 `basedOnGaps`、`relatedDimensions`、`referenceProducts`、`successMetrics`、`deliverables`、`risks`，并且 `referenceProducts` 统一替代旧语义里的 `benchmarkProducts`。随后新增 `src/app/api/runs/[runId]/stage-goals/route.ts`，提供 `POST` 入口：只有在 `run.scoring` 已存在时才允许生成；默认复用已有 persisted `scoring.gaps`；成功后把 `stageGoals` 写回 `run.stageGoals` 并返回最新 run。不要在这一步实现 handoff route，也不要引入新的 OpenAI 调用。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/stage-goals/build-stage-goals.test.ts src/app/api/runs/[runId]/stage-goals/route.test.ts</automated>
  </verify>
  <done>当前 run 已经可以从 persisted gaps 生成并保存固定三阶段目标，满足 STAG-01 和 STAG-02 的核心链路。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted `run.scoring.gaps` -> `run.stageGoals` | 阶段目标完全建立在 gap 之上，一旦映射错就会误导后续 handoff |
| repository update -> stale stage goals | 上游分析结果变化后，如果旧 `stageGoals` 没失效，会造成错误规划 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `buildStageGoals()` | mitigate | builder 只消费 persisted gaps，不重新推理 scoring；单测锁死固定三阶段和 gap 映射 |
| T-06-02 | Repudiation | repository invalidation | mitigate | 在 repository 层统一清空 stale `stageGoals`，并用测试证明上游变化后旧结果失效 |
</threat_model>

<verification>
- [ ] `npm run test:unit -- --run src/features/analysis-run/repository.test.ts src/features/stage-goals/build-stage-goals.test.ts src/app/api/runs/[runId]/stage-goals/route.test.ts` passes
- [ ] `StageGoal` 扩展字段能被 run mapper roundtrip
- [ ] `buildStageGoals()` 固定返回三阶段，不会生成第 4 个阶段
- [ ] `POST /api/runs/[runId]/stage-goals` 成功后返回 persisted `run.stageGoals`
</verification>

<success_criteria>
- `run.stageGoals` 已经从占位字段变成稳定的结构化结果
- 阶段目标生成完全基于 gap，而不是自由发挥式 roadmap
- stale `stageGoals` 不会在 scoring 或其上游变化后继续留存
</success_criteria>

<output>
After completion, create `.planning/phases/06-stage-goals-gsd-handoff/06-01-SUMMARY.md`
</output>
