---
phase: 04-explainable-scoring-gap-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0001_analysis_runs_scoring.sql
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.ts
  - src/features/analysis-run/repository.test.ts
autonomous: true
requirements: [SCOR-01]
user_setup: []
must_haves:
  truths:
    - "同一个 run 在 repository create/get/update roundtrip 之后，`run.scoring` 会完整保留，不会因 mapper 或 schema 漏字段而丢失。"
    - "`analysis_runs` 已有显式 `scoring` 持久化列，后续评分结果会写回 run 聚合，而不是落到旁路表。"
    - "`run.scoring` 契约已经预留 candidate scorecards 与 `gaps` 容器，后续 04-02/04-03 可以直接消费同一结构。"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "`analysis_runs.scoring` JSONB 列"
      contains: "scoring"
    - path: "src/features/analysis-run/types.ts"
      provides: "`AnalysisRunScoring` 契约"
      contains: "AnalysisRunScoring"
    - path: "src/features/analysis-run/repository.ts"
      provides: "run.scoring roundtrip 持久化入口"
      contains: "updateRun"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/features/analysis-run/run-mappers.ts"
      via: "scoring JSONB 列读写映射"
      pattern: "scoring"
    - from: "src/features/analysis-run/types.ts"
      to: "src/features/analysis-run/run-mappers.ts"
      via: "mapper 依照 `AnalysisRunScoring` 结构序列化"
      pattern: "AnalysisRunScoring|scoring"
    - from: "src/features/analysis-run/run-mappers.ts"
      to: "src/features/analysis-run/repository.ts"
      via: "repository roundtrip 保留 scoring 字段"
      pattern: "scoring"
---

<objective>
实现 Phase 4 的评分持久化地基：先固化 `run.scoring` 契约、数据库列和 repository roundtrip，不在这一计划里引入 evidence identity、评分生成路由或 gap/UI。
Purpose: 先把评分结果能稳定挂回 `analysis run` 这件事做实，后续 04-02 和 04-03 才能在同一份契约上继续堆评分与 gap。
Output: `AnalysisRunScoring` 类型、`analysis_runs.scoring` JSONB 列、mapper/repository roundtrip。
</objective>

<execution_context>
@C:/Users/84934/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/84934/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-explainable-scoring-gap-engine/04-CONTEXT.md
@.planning/phases/04-explainable-scoring-gap-engine/04-RESEARCH.md
@.planning/phases/04-explainable-scoring-gap-engine/04-VALIDATION.md
@src/db/schema.ts
@src/features/analysis-run/types.ts
@src/features/analysis-run/run-mappers.ts
@src/features/analysis-run/repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化 `run.scoring` 契约与数据库持久化列</name>
  <files>src/db/schema.ts, drizzle/0001_analysis_runs_scoring.sql, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts</files>
  <action>按 D-03 先在 `AnalysisRun` 上新增 `scoring` 契约，至少包含 candidate 级 scorecards、`overallScore`、`coverage`、`unknownCount` 与空的 `gaps` 容器，避免后续 04-02/04-03 再改 shared contract。同步修改 `src/db/schema.ts`，给 `analysis_runs` 增加 `scoring` JSONB 列，并新增对应 SQL migration；坚持单个 `analysis run` 聚合根，不新建独立评分表。`run-mappers` 要补齐 `scoring` 的序列化和反序列化，确保数据库层与类型层完全一致。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/analysis-run/repository.test.ts</automated>
  </verify>
  <done>`AnalysisRunScoring` 已成为显式共享契约，`analysis_runs` 已有 `scoring` 列，mapper 可以无损读写该字段。</done>
</task>

<task type="auto">
  <name>Task 2: 打通 repository roundtrip，确保 reopen 不丢 `run.scoring`</name>
  <files>src/features/analysis-run/repository.ts, src/features/analysis-run/repository.test.ts</files>
  <action>把 `run.scoring` 接入 repository 的 create/get/update roundtrip，重点验证重开 run 时不会丢失字段，也不会把 JSONB 内容默默截断。这里只处理 roundtrip，不提前实现 04-02 的失效清空逻辑；测试要明确覆盖 create 后读取、update 后读取，以及包含嵌套 scorecards / gaps 占位字段的完整对象。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/analysis-run/repository.test.ts</automated>
  </verify>
  <done>repository 已能稳定保存并恢复 `run.scoring`，为后续评分快照和 gap 结果提供可靠持久化基础。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> PostgreSQL `analysis_runs.scoring` | 持久化结构一旦漂移，reopen 就会拿到错误评分快照 |
| type contract -> mapper serialization | 类型层和 JSONB 层不一致会导致字段静默丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `run.scoring` contract | mitigate | 在 `types.ts`、`schema.ts`、`run-mappers.ts` 三层显式建模 `scoring`，并用 repository roundtrip 测试锁住字段一致性 |
| T-04-02 | Repudiation | repository roundtrip | mitigate | 用 `repository.test.ts` 覆盖 create/get/update + reopen 场景，证明 `run.scoring` 没有在持久化链路中被截断或回退 |
</threat_model>

<verification>
- [ ] `npm run test:unit -- --run src/features/analysis-run/repository.test.ts` passes
- [ ] `analysis_runs` 已有 `scoring` 持久化列与迁移文件
- [ ] `GET /api/runs/[runId]` 对应的 repository 读取能恢复写入过的 `run.scoring`
</verification>

<success_criteria>
- `run.scoring` 契约、数据库列、mapper 与 repository 已对齐
- 04-01 的 `files_modified` 只覆盖持久化地基，不再混入 scoring route、evidence identity 或 gap/UI
- 后续 04-02/04-03 可以直接复用同一份 `AnalysisRunScoring` 结构
</success_criteria>

<output>
After completion, create `.planning/phases/04-explainable-scoring-gap-engine/04-01-SUMMARY.md`
</output>
