---
phase: 04-explainable-scoring-gap-engine
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/features/scoring/build-gap-priorities.ts
  - src/features/scoring/build-gap-priorities.test.ts
  - src/app/api/runs/[runId]/scoring/route.ts
  - src/app/api/runs/[runId]/scoring/route.test.ts
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/run-shell.test.tsx
  - src/components/workspace/scoring-panel.tsx
  - src/components/workspace/scoring-panel.test.tsx
  - tests/e2e/scoring-gap-workflow.spec.ts
autonomous: true
requirements: [SCOR-01, SCOR-02]
user_setup: []
must_haves:
  truths:
    - "POST `/api/runs/[runId]/scoring` 返回的 `gaps` 列表里，每项都带 benchmark 候选、来源背景、`benchmarkEvidenceIds`，或在证据不足时返回 `status: \"unknown\"`。"
    - "Evidence 完成后，工作台通过 `POST /api/runs/[runId]/scoring` 生成持久化 `run.scoring`，并把返回 snapshot 回填当前 workspace state，而不是只在前端临时拼结果。"
    - "工作台在 Evidence 之后展示评分结果面板，首屏可见 `overallScore`、`coverage`、`unknownCount`、各维度摘要与 gap 摘要。"
    - "展开某个维度分数或 gap 后，可以看到 evidence contribution、`evidenceIds`、benchmark 标签与 matched modes。"
    - "重新打开 `/runs/[runId]` 时，面板恢复的是持久化 `run.scoring`，不是前端临时重算结果。"
  artifacts:
    - path: "src/features/scoring/build-gap-priorities.ts"
      provides: "gap priority 与 benchmark provenance 计算器"
      contains: "buildGapPriorities"
    - path: "src/components/workspace/run-shell.tsx"
      provides: "评分触发、持久化 `run.scoring` 回填与工作台状态刷新"
      contains: "runShell"
    - path: "src/components/workspace/scoring-panel.tsx"
      provides: "Phase 4 评分与 gap 解释面板"
      contains: "ScoringPanel"
    - path: "tests/e2e/scoring-gap-workflow.spec.ts"
      provides: "评分 -> gap -> reopen 主链路回归"
      contains: "scoring gap workflow"
  key_links:
    - from: "src/app/api/runs/[runId]/scoring/route.ts"
      to: "src/features/scoring/build-gap-priorities.ts"
      via: "在评分 route 里补齐 `run.scoring.gaps`"
      pattern: "buildGapPriorities"
    - from: "src/components/workspace/run-shell.tsx"
      to: "src/app/api/runs/[runId]/scoring/route.ts"
      via: "Evidence 完成后触发 `POST /api/runs/[runId]/scoring`，并用返回的持久化 `run.scoring` 刷新当前 workspace state"
      pattern: "fetch|/api/runs/|scoring"
    - from: "src/components/workspace/run-shell.tsx"
      to: "src/components/workspace/scoring-panel.tsx"
      via: "拿到持久化 `run.scoring` 后，把 snapshot 传给 `ScoringPanel` 并在 Evidence 之后展示"
      pattern: "ScoringPanel"
    - from: "tests/e2e/scoring-gap-workflow.spec.ts"
      to: "src/app/api/runs/[runId]/scoring/route.ts"
      via: "验证 Evidence 完成后生成评分、展示面板、gap 解释与 reopen 全链路"
      pattern: "/api/runs/"
---

<objective>
实现 Phase 4 的 gap priority 与解释表面：消费 04-02 已能生成的评分快照，补齐 benchmark provenance、工作台 explanation surface，并用聚焦 E2E 锁住主链路。
Purpose: 把 “score -> gap -> explanation -> reopen” 这段真正交付给用户的链路做完，同时保持 evidence-first 和 benchmark 可追溯。
Output: `buildGapPriorities()`、扩展后的 scoring route、ScoringPanel、聚焦 E2E。
</objective>

<execution_context>
@C:/Users/84934/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/84934/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-explainable-scoring-gap-engine/04-CONTEXT.md
@.planning/phases/04-explainable-scoring-gap-engine/04-RESEARCH.md
@.planning/phases/04-explainable-scoring-gap-engine/04-VALIDATION.md
@.planning/phases/04-explainable-scoring-gap-engine/04-02-PLAN.md
@src/app/api/runs/[runId]/scoring/route.ts
@src/components/workspace/run-shell.tsx
@src/components/workspace/evidence-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在评分 route 上补齐 gap priority 与 benchmark provenance</name>
  <files>src/features/scoring/build-gap-priorities.ts, src/features/scoring/build-gap-priorities.test.ts, src/app/api/runs/[runId]/scoring/route.ts, src/app/api/runs/[runId]/scoring/route.test.ts</files>
  <action>严格消费 04-02 已生成的 `run.scoring` snapshot，不再回头修改 shared scoring contract。按 D-07、D-08、D-09 实现 `buildGapPriorities()`：每个维度都以当前 evidence-backed 最强候选作为 benchmark，输出 `benchmarkCandidateId`、`benchmarkCandidateName`、`benchmarkMatchedModes`、`benchmarkEvidenceIds`、`benchmarkScore`、`gapSize`、`priority` 与 `summary`。如果 benchmark 本身证据不足，或当前候选该维度仍是 unknown，则 gap 返回 `status: "unknown"` 与 `priority: null`，禁止伪造结果。把 gap 列表持久化到 `run.scoring.gaps`，让 reopen 直接恢复。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/scoring/build-gap-priorities.test.ts src/app/api/runs/[runId]/scoring/route.test.ts</automated>
  </verify>
  <done>`run.scoring.gaps` 已成为可持久化、可追 benchmark 来源的结构化输出。</done>
</task>

<task type="auto">
  <name>Task 2: 在工作台接入可展开的评分与 gap explanation 面板</name>
  <files>src/components/workspace/run-shell.tsx, src/components/workspace/run-shell.test.tsx, src/components/workspace/scoring-panel.tsx, src/components/workspace/scoring-panel.test.tsx</files>
  <action>按 D-04、D-05、D-06、D-10、D-11、D-12 在 `run-shell.tsx` 接入评分生成入口：仅当当前 run 已有 evidence 时，提供“生成评分/重新计算”动作，触发 `POST /api/runs/[runId]/scoring`。请求成功后，用响应里已持久化的 `run.scoring` 替换当前 workspace state 中的 `run.scoring`，再把该 snapshot 传给 `ScoringPanel`；禁止在组件里基于 candidates/evidence 临时重算。`ScoringPanel` 挂在 Evidence 之后，首屏只展示候选 `overallScore`、`coverage`、`unknownCount`、dimension 摘要与 gap 摘要；展开后再展示 evidence contribution、`evidenceIds`、benchmark 标签、`benchmarkMatchedModes` 和 summary。保留 Phase 4 边界，不新增公式大面板、雷达图、关系图，也不提前进入 Phase 6 的 Stage Goals。</action>
  <verify>
    <automated>npm run test:unit -- --run src/components/workspace/run-shell.test.tsx src/components/workspace/scoring-panel.test.tsx</automated>
  </verify>
  <done>用户可以在 Evidence 完成后从工作台生成评分、看到持久化结果回填，并直接查看和展开评分与 gap 解释，不需要读原始 JSON。</done>
</task>

<task type="auto">
  <name>Task 3: 用聚焦 E2E 锁住 scoring -> gap -> reopen 主链路</name>
  <files>tests/e2e/scoring-gap-workflow.spec.ts</files>
  <action>新增 `tests/e2e/scoring-gap-workflow.spec.ts`，在 `MOCK_OPENAI=true` 下覆盖 `GoalCard -> Dimensions -> SearchPlan -> Candidates -> Evidence -> Scoring -> Gap -> Reopen`。断言至少包括：Evidence 完成后用户可以触发评分；工作台用 `POST /api/runs/[runId]/scoring` 返回的持久化 snapshot 显示 `overallScore`、`coverage` 与 `unknownCount`；unknown 维度不被当成低分；gap 行带 benchmark 标签；展开后能看到 evidence contribution / `evidenceIds`；重新打开 `/runs/[runId]` 后 scoring 与 gaps 仍从持久化 snapshot 恢复。任务级只跑这条聚焦 spec，全量 `npm run check && npm run test:e2e` 留到 wave/final 验证。</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/scoring-gap-workflow.spec.ts</automated>
  </verify>
  <done>Phase 4 有一条专门覆盖评分、gap 与 reopen 的自动化回归，避免主链路回归只靠手测。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoring route -> persisted `run.scoring.gaps` | gap 结果一旦写回 run，reopen 会直接消费 |
| browser workspace -> `ScoringPanel` | UI 只应展示结构化解释数据，不能自行发明业务真相 |
| focused E2E -> reopen flow | 自动化需要证明 reopen 读取的是持久化结果，而非前端临时状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | benchmark provenance | mitigate | `buildGapPriorities()` 必须显式持久化 `benchmarkCandidateId`、`benchmarkMatchedModes` 与 `benchmarkEvidenceIds`，禁止只存 summary 文本 |
| T-04-07 | Information Disclosure | explanation panel | mitigate | `ScoringPanel` 只展示已有结构化字段和必要 excerpt，不拼接额外原始页面全文 |
| T-04-08 | Repudiation | reopen scoring/gaps | mitigate | 用聚焦 E2E 断言 reopen 后仍显示持久化 scoring/gaps，证明结果不是前端临时状态 |
</threat_model>

<verification>
- [ ] `npm run test:unit -- --run src/features/scoring/build-gap-priorities.test.ts src/components/workspace/run-shell.test.tsx src/components/workspace/scoring-panel.test.tsx` passes
- [ ] `npm run test:e2e -- tests/e2e/scoring-gap-workflow.spec.ts` passes
- [ ] Evidence 完成后可从工作台触发评分，并用返回的 persisted `run.scoring` 刷新当前面板
- [ ] `/runs/[runId]` 重开后仍展示同一份 persisted scoring/gap 输出
- [ ] wave/final 边界按 `04-VALIDATION.md` 执行 `npm run check && npm run test:e2e`
</verification>

<success_criteria>
- gap 结果完全建立在 04-02 已生成的 scoring snapshot 上，不回头扩大 04-01/04-02 范围
- 每个 gap 都能说明 benchmark 是谁、来自哪些 matched modes、引用哪些 `benchmarkEvidenceIds`
- 工作台可在 Evidence 完成后生成评分、回填持久化结果，并展开查看维度分与 gap 解释
- 04-03 只覆盖 gap priority、解释表面和聚焦 E2E，不扩到 Phase 5/6 的可视化和阶段目标
</success_criteria>

<output>
After completion, create `.planning/phases/04-explainable-scoring-gap-engine/04-03-SUMMARY.md`
</output>
