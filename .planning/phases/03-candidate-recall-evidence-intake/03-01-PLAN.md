---
phase: 03-candidate-recall-evidence-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.test.ts
  - src/features/candidate-recall/candidate-schema.ts
  - src/features/candidate-recall/candidate-schema.test.ts
  - src/features/candidate-recall/generate-candidate-drafts.ts
  - src/features/candidate-recall/generate-candidate-drafts.test.ts
  - src/features/candidate-recall/normalize-candidates.ts
  - src/features/candidate-recall/normalize-candidates.test.ts
  - src/features/candidate-recall/select-top-candidates.ts
  - src/features/candidate-recall/select-top-candidates.test.ts
  - src/app/api/runs/[runId]/route.ts
  - src/app/api/runs/[runId]/dimensions/route.ts
  - src/app/api/runs/[runId]/search-plan/route.ts
  - src/app/api/runs/[runId]/candidates/route.ts
  - src/app/api/runs/[runId]/candidates/route.test.ts
autonomous: true
requirements: [SRCH-03, SRCH-04]
user_setup: []
must_haves:
  truths:
    - "Phase 3 must persist normalized candidates on the analysis run before evidence generation starts."
    - "Candidate identity must be stable across recall queries, with matched modes merged instead of duplicated."
    - "Top-5 deep-dive selection must be deterministic and bounded."
  artifacts:
    - path: "src/features/candidate-recall/generate-candidate-drafts.ts"
      provides: "candidate recall draft generation via Responses API web_search"
      contains: "generateCandidateDrafts"
    - path: "src/features/candidate-recall/normalize-candidates.ts"
      provides: "candidate dedupe and normalization rules"
      contains: "normalizeCandidates"
    - path: "src/features/candidate-recall/select-top-candidates.ts"
      provides: "deterministic top-5 deep-dive selection"
      contains: "selectTopCandidates"
    - path: "src/app/api/runs/[runId]/candidates/route.ts"
      provides: "candidate generation endpoint with status gating"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/runs/[runId]/candidates/route.ts"
      to: "src/features/candidate-recall/generate-candidate-drafts.ts"
      via: "generate raw candidate drafts from confirmed search plan"
      pattern: "generateCandidateDrafts"
    - from: "src/app/api/runs/[runId]/candidates/route.ts"
      to: "src/features/candidate-recall/normalize-candidates.ts"
      via: "merge duplicate products into stable candidate records"
      pattern: "normalizeCandidates"
    - from: "src/app/api/runs/[runId]/candidates/route.ts"
      to: "src/features/candidate-recall/select-top-candidates.ts"
      via: "mark deep-dive candidate subset"
      pattern: "selectTopCandidates"
---

<objective>
Implement candidate recall for Phase 3 using the confirmed SearchPlan, local normalization, and deterministic top-5 deep-dive selection. Purpose: turn SearchPlan from a draft artifact into persisted candidate objects that can support evidence intake. Output: recall schema and service helpers, candidate persistence on the analysis run, route-level gating, and upstream invalidation that clears stale candidates and evidence when Goal, Dimensions, or SearchPlan changes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md
@.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md
@.planning/phases/03-candidate-recall-evidence-intake/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: extend the run aggregate for candidate persistence and invalidation</name>
  <files>src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.test.ts, src/app/api/runs/[runId]/route.ts, src/app/api/runs/[runId]/dimensions/route.ts, src/app/api/runs/[runId]/search-plan/route.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/app/api/runs/[runId]/search-plan/route.ts</read_first>
  <action>Extend the run types so Candidate records can store `sources`, `matchedQueries`, and `recallRank`, and extend `AnalysisRunStatus` to include `candidates_ready` and `evidence_ready`. Update mapper validation so only well-formed candidates and evidence survive rehydration. When Goal, Dimensions, or SearchPlan changes, clear stale `candidates` and `evidence`, and roll status back to the newest valid upstream stage instead of leaving downstream artifacts attached.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/analysis-run/types.ts` contains `candidates_ready`
    - `src/features/analysis-run/types.ts` contains `evidence_ready`
    - `src/features/analysis-run/types.ts` contains `matchedQueries`
    - `src/features/analysis-run/run-mappers.ts` validates candidate `sources`
    - `src/features/analysis-run/repository.test.ts` covers downstream invalidation
  </acceptance_criteria>
  <done>The analysis run can safely persist candidate artifacts and clear stale downstream data when upstream inputs change.</done>
</task>

<task type="auto">
  <name>Task 2: implement candidate recall draft generation and local normalization</name>
  <files>src/features/candidate-recall/candidate-schema.ts, src/features/candidate-recall/candidate-schema.test.ts, src/features/candidate-recall/generate-candidate-drafts.ts, src/features/candidate-recall/generate-candidate-drafts.test.ts, src/features/candidate-recall/normalize-candidates.ts, src/features/candidate-recall/normalize-candidates.test.ts, src/features/candidate-recall/select-top-candidates.ts, src/features/candidate-recall/select-top-candidates.test.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md, src/features/search-plan/build-search-plan-input.ts, src/features/search-plan/generate-search-plan.ts</read_first>
  <action>Create a candidate draft schema that only allows source types `official_site`, `docs`, `pricing`, and `review`. Implement `generateCandidateDrafts()` using Responses API plus `web_search`, with a stable mock path under `MOCK_OPENAI=true`. Implement `normalizeCandidates()` so dedupe prefers official-site domain, then falls back to normalized product name, while merging `matchedModes`, `strengthDimensions`, `sources`, and `matchedQueries`. Implement `selectTopCandidates()` so ranking prioritizes `same_goal`, then evidence readiness proxies such as official presence and official-source count, then dimension coverage. Keep the deep-dive set fixed to the top 5 entries.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/candidate-recall/candidate-schema.ts` contains `official_site`
    - `src/features/candidate-recall/generate-candidate-drafts.ts` contains `web_search`
    - `src/features/candidate-recall/generate-candidate-drafts.ts` contains `MOCK_OPENAI`
    - `src/features/candidate-recall/normalize-candidates.ts` contains domain-based dedupe logic
    - `src/features/candidate-recall/select-top-candidates.ts` enforces a top-5 limit
  </acceptance_criteria>
  <done>Candidate recall is no longer a raw search result list; it becomes a stable, testable candidate set with deterministic deep-dive selection.</done>
</task>

<task type="auto">
  <name>Task 3: expose a candidate generation endpoint gated by confirmed SearchPlan</name>
  <files>src/app/api/runs/[runId]/candidates/route.ts, src/app/api/runs/[runId]/candidates/route.test.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md, src/features/analysis-run/repository.ts, src/app/api/runs/[runId]/search-plan/route.ts, src/features/candidate-recall/generate-candidate-drafts.ts</read_first>
  <action>Add `POST /api/runs/[runId]/candidates` with the following rules: require an existing run whose SearchPlan has been confirmed; return 400 when SearchPlan is missing or not confirmed; if normalized candidates already exist and `forceRegenerate` is not set, return the persisted result; otherwise generate candidate drafts, normalize and rank them, persist the full ordered list, mark deep-dive candidates, clear stale evidence, and move the run status to `candidates_ready`. Keep the response shape stable enough for the workspace to reuse without recomputing rank client-side.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/app/api/runs/[runId]/candidates/route.ts` contains `forceRegenerate`
    - `src/app/api/runs/[runId]/candidates/route.ts` contains `candidates_ready`
    - `src/app/api/runs/[runId]/candidates/route.ts` returns 400 on missing or unconfirmed SearchPlan
    - `src/app/api/runs/[runId]/candidates/route.test.ts` covers cache-hit and regenerate paths
  </acceptance_criteria>
  <done>The workspace can trigger candidate recall exactly once per confirmed SearchPlan and get back stable, persisted results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser candidate actions -> `/api/runs/[runId]/candidates`
- Next.js server -> OpenAI Responses API web_search
- Next.js server -> PostgreSQL analysis run aggregate

## STRIDE Register
- threat_id: T-03-01
  category: tampering
  component: candidate_payload
  disposition: mitigate
  mitigation_plan: "Validate candidate drafts against a strict schema before normalization, and only persist normalized records."
- threat_id: T-03-02
  category: spoofing
  component: candidate_identity
  disposition: mitigate
  mitigation_plan: "Use official-site domain as the primary dedupe key and normalized product name only as a fallback, while preserving merged matched modes."
- threat_id: T-03-03
  category: denial_of_service
  component: candidate_regeneration
  disposition: mitigate
  mitigation_plan: "Short-circuit candidate regeneration unless `forceRegenerate` is explicitly requested, and bound deep-dive selection to top 5."
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `POST /api/runs/[runId]/candidates` returns 400 when SearchPlan is missing or not confirmed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Candidate recall satisfies `SRCH-03` and `SRCH-04` with deterministic persistence and invalidation
  </success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-recall-evidence-intake/03-01-SUMMARY.md`
</output>
