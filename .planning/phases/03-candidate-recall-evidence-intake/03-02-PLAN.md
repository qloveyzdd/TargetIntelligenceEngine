---
phase: 03-candidate-recall-evidence-intake
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/features/evidence/schema.ts
  - src/features/evidence/schema.test.ts
  - src/features/evidence/build-evidence-source-tasks.ts
  - src/features/evidence/build-evidence-source-tasks.test.ts
  - src/features/evidence/load-page-text.ts
  - src/features/evidence/load-page-text.test.ts
  - src/features/evidence/extract-evidence.ts
  - src/features/evidence/extract-evidence.test.ts
  - src/app/api/runs/[runId]/evidence/route.ts
  - src/app/api/runs/[runId]/evidence/route.test.ts
autonomous: true
requirements: [EVID-01, EVID-02, SRCH-04]
user_setup: []
must_haves:
  truths:
    - "Evidence intake must only run against the persisted top-5 candidate set."
    - "Each evidence record must preserve candidate, dimension, source, excerpt, extracted value, confidence, and capture time."
    - "Official sources must be queued before review sources."
  artifacts:
    - path: "src/features/evidence/build-evidence-source-tasks.ts"
      provides: "official-first evidence source queue construction"
      contains: "buildEvidenceSourceTasks"
    - path: "src/features/evidence/load-page-text.ts"
      provides: "page text loading with fetch-first and Playwright fallback"
      contains: "loadPageText"
    - path: "src/features/evidence/extract-evidence.ts"
      provides: "structured evidence extraction"
      contains: "extractEvidence"
    - path: "src/app/api/runs/[runId]/evidence/route.ts"
      provides: "evidence generation endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/runs/[runId]/evidence/route.ts"
      to: "src/features/evidence/build-evidence-source-tasks.ts"
      via: "derive bounded source tasks from top-5 candidates"
      pattern: "buildEvidenceSourceTasks"
    - from: "src/app/api/runs/[runId]/evidence/route.ts"
      to: "src/features/evidence/load-page-text.ts"
      via: "load source page text before extraction"
      pattern: "loadPageText"
    - from: "src/app/api/runs/[runId]/evidence/route.ts"
      to: "src/features/evidence/extract-evidence.ts"
      via: "turn page text into evidence records"
      pattern: "extractEvidence"
---

<objective>
Implement evidence intake for Phase 3 by turning the persisted top-5 candidate subset into source tasks, page text payloads, and fine-grained evidence records. Purpose: make evidence a first-class persisted object rather than a future placeholder. Output: evidence schema and extraction helpers, a fetch-first loader with bounded Playwright fallback, and an endpoint that writes fine-grained evidence back to the analysis run.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md
@.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md
@.planning/phases/03-candidate-recall-evidence-intake/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: define evidence schema and build the official-first source queue</name>
  <files>src/features/evidence/schema.ts, src/features/evidence/schema.test.ts, src/features/evidence/build-evidence-source-tasks.ts, src/features/evidence/build-evidence-source-tasks.test.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md, src/features/analysis-run/types.ts</read_first>
  <action>Create a strict evidence schema that requires `candidateId`, `dimensionId`, `sourceType`, `url`, `excerpt`, `extractedValue`, `confidence`, and `capturedAt`. Implement `buildEvidenceSourceTasks()` so it only selects the persisted top-5 candidates, generates tasks in the order `official_site`, `docs`, `pricing`, `review`, and scopes each task to `candidate + dimension + source URL`. Deduplicate repeated URLs without losing the dimension relationship, so later extraction stays evidence-granular instead of falling back to candidate-level summaries.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/evidence/schema.ts` contains `capturedAt`
    - `src/features/evidence/schema.ts` contains `extractedValue`
    - `src/features/evidence/build-evidence-source-tasks.ts` contains `official_site`
    - `src/features/evidence/build-evidence-source-tasks.ts` enforces a top-5 candidate limit
    - `src/features/evidence/build-evidence-source-tasks.test.ts` covers URL dedupe
  </acceptance_criteria>
  <done>The system has a bounded, official-first queue of evidence tasks with a stable fine-grained schema.</done>
</task>

<task type="auto">
  <name>Task 2: load page text and extract structured evidence records</name>
  <files>src/features/evidence/load-page-text.ts, src/features/evidence/load-page-text.test.ts, src/features/evidence/extract-evidence.ts, src/features/evidence/extract-evidence.test.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md, package.json</read_first>
  <action>Implement `loadPageText()` with native `fetch` as the default path. Only fall back to Playwright when fetch returns empty, obviously too-thin content, or a page that requires client-side rendering for the main text. Implement `extractEvidence()` using Structured Outputs so the model returns strict evidence records tied to a single candidate and dimension. Under `MOCK_OPENAI=true`, return stable evidence mocks to keep unit and E2E runs deterministic. Keep extraction output small and source-bound so evidence never turns into a loose narrative summary.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/evidence/load-page-text.ts` contains a fetch-first code path
    - `src/features/evidence/load-page-text.ts` contains a Playwright fallback path
    - `src/features/evidence/extract-evidence.ts` contains `MOCK_OPENAI`
    - `src/features/evidence/extract-evidence.ts` uses a strict schema for extraction output
    - `src/features/evidence/extract-evidence.test.ts` covers evidence field completeness
  </acceptance_criteria>
  <done>Evidence extraction can run without external test flakiness and still preserve the fine-grained structure required by later phases.</done>
</task>

<task type="auto">
  <name>Task 3: expose an evidence generation endpoint driven by persisted candidates</name>
  <files>src/app/api/runs/[runId]/evidence/route.ts, src/app/api/runs/[runId]/evidence/route.test.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md, src/features/analysis-run/repository.ts, src/features/evidence/build-evidence-source-tasks.ts</read_first>
  <action>Add `POST /api/runs/[runId]/evidence` with the following rules: require a run in `candidates_ready` or `evidence_ready`; return 400 if persisted candidates are missing; if evidence already exists and `forceRegenerate` is not set, return the stored evidence; otherwise build source tasks from the top-5 candidates, load page text, extract evidence, persist the fine-grained list, and move the run status to `evidence_ready`. Keep the implementation conservative: do not deep-dive more than the top 5 candidates, and do not allow review sources to replace missing official sources by default.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/app/api/runs/[runId]/evidence/route.ts` contains `forceRegenerate`
    - `src/app/api/runs/[runId]/evidence/route.ts` contains `evidence_ready`
    - `src/app/api/runs/[runId]/evidence/route.ts` returns 400 when candidates are missing
    - `src/app/api/runs/[runId]/evidence/route.test.ts` covers cache-hit and regenerate flows
  </acceptance_criteria>
  <done>The workspace can turn a persisted candidate set into persisted evidence without introducing unlimited crawling behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser evidence actions -> `/api/runs/[runId]/evidence`
- Next.js server -> external source pages
- Next.js server -> OpenAI Structured Outputs
- Next.js server -> PostgreSQL analysis run aggregate

## STRIDE Register
- threat_id: T-03-04
  category: tampering
  component: evidence_payload
  disposition: mitigate
  mitigation_plan: "Validate extraction output against a strict evidence schema before persistence, and reject incomplete records."
- threat_id: T-03-05
  category: information_disclosure
  component: page_text_handling
  disposition: mitigate
  mitigation_plan: "Only persist the structured evidence fields needed by the product, not arbitrary full-page dumps."
- threat_id: T-03-06
  category: denial_of_service
  component: dynamic_page_fallback
  disposition: mitigate
  mitigation_plan: "Use fetch first, keep Playwright as a bounded fallback, and restrict evidence intake to top-5 candidates."
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `POST /api/runs/[runId]/evidence` returns 400 when candidates are missing
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Evidence intake satisfies `EVID-01`, `EVID-02`, and the top-5 bound in `SRCH-04`
  </success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-recall-evidence-intake/03-02-SUMMARY.md`
</output>
