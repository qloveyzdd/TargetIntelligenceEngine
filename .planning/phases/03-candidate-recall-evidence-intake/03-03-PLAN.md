---
phase: 03-candidate-recall-evidence-intake
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/candidates-panel.tsx
  - src/components/workspace/evidence-panel.tsx
  - src/components/workspace/analysis-placeholders.tsx
  - tests/e2e/candidate-evidence-workflow.spec.ts
autonomous: true
requirements: [SRCH-03, SRCH-04, EVID-01, EVID-02]
user_setup: []
must_haves:
  truths:
    - "Candidates and evidence must stop being placeholders after Phase 3."
    - "The workspace must clearly show which candidates belong to the top-5 deep-dive set."
    - "Reopening a run must rehydrate persisted candidates and evidence instead of recomputing them."
  artifacts:
    - path: "src/components/workspace/candidates-panel.tsx"
      provides: "candidate generation and deep-dive set display"
      contains: "CandidatesPanel"
    - path: "src/components/workspace/evidence-panel.tsx"
      provides: "evidence generation and grouped evidence display"
      contains: "EvidencePanel"
    - path: "tests/e2e/candidate-evidence-workflow.spec.ts"
      provides: "end-to-end coverage for the candidate-to-evidence workflow"
      contains: "candidate-evidence workflow"
  key_links:
    - from: "src/components/workspace/run-shell.tsx"
      to: "src/components/workspace/candidates-panel.tsx"
      via: "workspace composition for candidate generation"
      pattern: "CandidatesPanel"
    - from: "src/components/workspace/run-shell.tsx"
      to: "src/components/workspace/evidence-panel.tsx"
      via: "workspace composition for evidence generation"
      pattern: "EvidencePanel"
    - from: "tests/e2e/candidate-evidence-workflow.spec.ts"
      to: "src/app/api/runs/[runId]/candidates/route.ts"
      via: "candidate generation in the full workflow"
      pattern: "/api/runs/"
    - from: "tests/e2e/candidate-evidence-workflow.spec.ts"
      to: "src/app/api/runs/[runId]/evidence/route.ts"
      via: "evidence generation in the full workflow"
      pattern: "/api/runs/"
---

<objective>
Integrate persisted candidates and evidence into the workspace so Phase 3 delivers a visible end-to-end slice rather than hidden backend state. Purpose: replace placeholders with real workflow steps the user can trigger, inspect, and revisit. Output: candidate and evidence panels, placeholder cleanup, and an end-to-end test that covers generation, persistence, and run reopen behavior under `MOCK_OPENAI=true`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md
@.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md
@.planning/phases/03-candidate-recall-evidence-intake/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: wire candidate generation into the workspace and surface the deep-dive set</name>
  <files>src/components/workspace/run-shell.tsx, src/components/workspace/candidates-panel.tsx</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-CONTEXT.md, src/components/workspace/run-shell.tsx, src/components/workspace/analysis-placeholders.tsx</read_first>
  <action>Add a dedicated `CandidatesPanel` that calls `POST /api/runs/[runId]/candidates`, supports both generate and regenerate flows, and renders candidates in persisted `recallRank` order. Show `matchedModes`, `strengthDimensions`, `officialUrl`, and a clear label for the top-5 deep-dive set so the user understands which products will receive evidence intake. Update the workspace shell to fetch and pass persisted candidate state without recomputing ranking client-side.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/components/workspace/candidates-panel.tsx` contains both generate and regenerate actions
    - `src/components/workspace/candidates-panel.tsx` surfaces the top-5 deep-dive set
    - `src/components/workspace/run-shell.tsx` renders `CandidatesPanel`
  </acceptance_criteria>
  <done>The user can trigger candidate recall from the workspace and immediately see the ordered, persisted candidate list.</done>
</task>

<task type="auto">
  <name>Task 2: wire evidence generation into the workspace and retire stale placeholders</name>
  <files>src/components/workspace/run-shell.tsx, src/components/workspace/evidence-panel.tsx, src/components/workspace/analysis-placeholders.tsx</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-RESEARCH.md, src/components/workspace/run-shell.tsx, src/features/analysis-run/types.ts</read_first>
  <action>Add an `EvidencePanel` that stays disabled until candidates exist, then calls `POST /api/runs/[runId]/evidence` for generate and regenerate flows. Group the resulting evidence by candidate and then by dimension, and display `sourceType`, clickable `url`, `excerpt`, `extractedValue`, and `confidence`. Remove the Phase 1 placeholder behavior for Candidates and Evidence, but keep Stage Goals as the remaining placeholder for later phases.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/components/workspace/evidence-panel.tsx` contains both generate and regenerate actions
    - `src/components/workspace/evidence-panel.tsx` renders grouped evidence records
    - `src/components/workspace/analysis-placeholders.tsx` keeps only the Stage Goals placeholder
    - `src/components/workspace/run-shell.tsx` renders `EvidencePanel`
  </acceptance_criteria>
  <done>The workspace shows real evidence instead of placeholders, while keeping the remaining future scope clearly separated.</done>
</task>

<task type="auto">
  <name>Task 3: prove the candidate-to-evidence flow with one stable end-to-end path</name>
  <files>tests/e2e/candidate-evidence-workflow.spec.ts</files>
  <read_first>.planning/phases/03-candidate-recall-evidence-intake/03-VALIDATION.md, tests/e2e</read_first>
  <action>Create an E2E test that covers the full Phase 3 path: generate and confirm GoalCard, generate and confirm Dimensions, generate and confirm SearchPlan, generate Candidates, generate Evidence, then reopen `/runs/[runId]` and verify both candidate and evidence data rehydrate from persistence. Keep the test deterministic by running under `MOCK_OPENAI=true` and by asserting persisted UI content rather than transient loading copy.</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - `tests/e2e/candidate-evidence-workflow.spec.ts` covers candidate generation
    - `tests/e2e/candidate-evidence-workflow.spec.ts` covers evidence generation
    - `tests/e2e/candidate-evidence-workflow.spec.ts` verifies run reopen behavior
  </acceptance_criteria>
  <done>Phase 3 has one stable end-to-end contract that proves candidate recall and evidence persistence work together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser workspace actions -> candidate and evidence route handlers
- Browser rendering -> persisted run payload returned from the server

## STRIDE Register
- threat_id: T-03-07
  category: tampering
  component: workspace_candidate_flow
  disposition: mitigate
  mitigation_plan: "Use server-persisted candidate and evidence payloads as the source of truth, not client-side recomputation."
- threat_id: T-03-08
  category: information_disclosure
  component: evidence_display
  disposition: mitigate
  mitigation_plan: "Display only the structured evidence fields required by the product and link back to the source URL for deeper inspection."
- threat_id: T-03-09
  category: denial_of_service
  component: phase_3_e2e
  disposition: mitigate
  mitigation_plan: "Keep the E2E path on mock OpenAI responses and one bounded workflow to avoid flaky network dependence."
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `npm run test:e2e` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The workspace exposes persisted candidates and evidence end-to-end and satisfies `SRCH-03`, `SRCH-04`, `EVID-01`, and `EVID-02`
  </success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-recall-evidence-intake/03-03-SUMMARY.md`
</output>
