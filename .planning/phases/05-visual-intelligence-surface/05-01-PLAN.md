---
phase: 05-visual-intelligence-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/visuals/radar-types.ts
  - src/features/visuals/build-radar-chart-model.ts
  - src/features/visuals/build-radar-chart-model.test.ts
  - src/features/visuals/select-radar-candidates.ts
  - src/features/visuals/select-radar-candidates.test.ts
autonomous: true
requirements: [VIZ-01]
user_setup: []
must_haves:
  truths:
    - "雷达图数据只来自 persisted `run.scoring`，不会绕过 Phase 4 评分链重新计算。"
    - "默认比较集合固定为当前目标加 overall score 最高的前 3 个已评分候选，且支持后续手动增减。"
    - "unknown 维度保持缺证据状态，不会在雷达图模型里被伪装成 0 分或低分。"
  artifacts:
    - path: "src/features/visuals/radar-types.ts"
      provides: "雷达图派生模型与选择状态契约"
      contains: "RadarChartModel"
    - path: "src/features/visuals/build-radar-chart-model.ts"
      provides: "从 `run.scoring` 构建雷达图轴与系列的纯函数"
      contains: "buildRadarChartModel"
    - path: "src/features/visuals/select-radar-candidates.ts"
      provides: "默认 top 3 与手动增减候选的确定性选择逻辑"
      contains: "selectRadarCandidates"
  key_links:
    - from: "src/features/scoring/build-scoring-snapshot.ts"
      to: "src/features/visuals/build-radar-chart-model.ts"
      via: "消费 candidate scorecards 与 dimension scorecards 生成雷达投影视图"
      pattern: "candidateScorecards|dimensionScorecards"
    - from: "src/features/visuals/select-radar-candidates.ts"
      to: "src/features/visuals/build-radar-chart-model.ts"
      via: "先确定可见候选集合，再输出雷达系列顺序"
      pattern: "selectedCandidateIds|defaultCandidateIds"
---

<objective>
冻结 Phase 5 雷达图的派生契约和比较规则，让后续 ECharts 挂载只负责渲染，不再在 UI 层发明业务逻辑。

Purpose: 在不破坏当前单页工作台和 Phase 4 explainable scoring 的前提下，把 “目标 + top 3 scored candidates + 手动增减” 先固化成纯函数与测试。
Output: `RadarChartModel` 契约、默认/手动候选选择器、雷达图数据 builder 与对应单测。
</objective>

<execution_context>
@C:/Users/84934/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/84934/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-visual-intelligence-surface/05-CONTEXT.md
@.planning/phases/05-visual-intelligence-surface/05-RESEARCH.md
@.planning/phases/05-visual-intelligence-surface/05-VALIDATION.md
@.planning/phases/04-explainable-scoring-gap-engine/04-03-SUMMARY.md
@src/features/analysis-run/types.ts
@src/features/scoring/build-scoring-snapshot.ts
@src/components/workspace/run-shell.tsx

<interfaces>
From `src/features/analysis-run/types.ts`:
```ts
export type DimensionScoreStatus = "known" | "unknown";

export type DimensionScorecard = {
  candidateId: string;
  dimensionId: string;
  status: DimensionScoreStatus;
  score: number | null;
  coverage: number;
  evidenceIds: string[];
  summary: string;
};

export type CandidateScorecard = {
  candidateId: string;
  overallScore: number | null;
  coverage: number;
  unknownCount: number;
  dimensionScorecards: DimensionScorecard[];
};

export type AnalysisRunScoring = {
  generatedAt: string;
  candidateScorecards: CandidateScorecard[];
  gaps: GapPriority[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化雷达图投影契约与默认比较规则</name>
  <files>src/features/visuals/radar-types.ts, src/features/visuals/build-radar-chart-model.ts, src/features/visuals/build-radar-chart-model.test.ts</files>
  <behavior>
    - Test 1: `buildRadarChartModel()` 默认输出目标系列和 overall score 最高的前 3 个已评分候选，顺序稳定且不包含未评分候选。
    - Test 2: 维度分数为 `unknown` 时，builder 保留 `null` 或等价缺失标记，并额外暴露缺证据元数据，而不是写成 `0`。
    - Test 3: builder 只消费 `run.scoring`、`run.dimensions`、`run.candidates`，不会触发新的评分或 gap 计算。
  </behavior>
  <action>新增 `src/features/visuals/radar-types.ts` 与 `src/features/visuals/build-radar-chart-model.ts`，把雷达图需要的轴、系列、系列元数据、默认候选列表、unknown 维度提示都固化为纯 TypeScript 契约。按 D-01、D-03 实现：默认比较对象必须是 “当前目标 + overall score 最高的前 3 个已评分候选”；只消费已存在的 `run.scoring.candidateScorecards`；禁用维度不进雷达轴；unknown 维度保留缺失值和 `evidenceIds`/coverage 摘要，禁止降级成 0 分。这里不要引入 ECharts，也不要改 `run-shell.tsx`，保持这一计划只定义业务投影逻辑。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/visuals/build-radar-chart-model.test.ts</automated>
  </verify>
  <done>雷达图的数据源、默认比较顺序和 unknown 表达方式已经被纯函数锁定，后续 UI 只能复用该模型而不能各自解释。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固化手动增减候选的选择器，不改变评分真相</name>
  <files>src/features/visuals/select-radar-candidates.ts, src/features/visuals/select-radar-candidates.test.ts</files>
  <behavior>
    - Test 1: 默认状态下返回目标加 top 3 已评分候选。
    - Test 2: 手动添加候选时，只允许加入已评分候选，目标系列始终固定存在。
    - Test 3: 手动移除候选只影响可见系列集合，不会改写 score、coverage 或 gap 数据。
  </behavior>
  <action>新增 `src/features/visuals/select-radar-candidates.ts`，实现与 UI 无关的候选选择器，按 D-02 保证用户可以在默认 top 3 之上手动增减候选，但首屏不依赖手动选择才出图。选择器要做的只是确定 `selectedCandidateIds` 与稳定排序，不做任何评分重算；对没有 `overallScore` 的候选直接拒绝进入可视集合；目标系列始终固定为第一个。这样 05-03 只要接控件和事件，不需要再思考比较逻辑。</action>
  <verify>
    <automated>npm run test:unit -- --run src/features/visuals/select-radar-candidates.test.ts</automated>
  </verify>
  <done>雷达图的手动增减候选规则已经被单测锁死，后续交互层只能切换可见系列，不能影响 Phase 4 的评分结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run.scoring` -> radar projection | persisted 评分快照被投影为雷达图系列，任何错误映射都会误导用户 |
| candidate selector -> visible radar series | UI 选择器只能切换显示对象，不能成为新的评分入口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `buildRadarChartModel()` | mitigate | 对 unknown 维度保留 `null`/缺失标记与元数据，禁止把缺证据映射成 `0` 分；单测覆盖 D-03。 |
| T-05-02 | Repudiation | `selectRadarCandidates()` | mitigate | 选择器只返回可见候选 ID，不接触 score/gap 计算；测试断言手动增减不会改写评分快照。 |
| T-05-03 | Information Disclosure | radar model metadata | accept | 雷达模型仅携带 dimension/evidence 摘要级元数据，不直接展开 evidence excerpt，详细证据留给 05-03 共享解释面板。 |
</threat_model>

<verification>
- [ ] `npm run test:unit -- --run src/features/visuals/build-radar-chart-model.test.ts src/features/visuals/select-radar-candidates.test.ts` passes
- [ ] builder 默认返回目标加 top 3 已评分候选，且顺序稳定
- [ ] unknown 维度在模型里保持缺失语义，没有任何 `0` 分回填
- [ ] 手动增减逻辑只影响可见系列，不影响 persisted scoring
</verification>

<success_criteria>
- Radar 派生逻辑已经独立成纯函数，可被 05-03 的 ECharts 组件直接消费
- D-01、D-02、D-03 在代码层已有明确契约，不需要执行期再临时决定
- 05-01 不修改 `run-shell.tsx`、不新增 route、不中断当前单页工作台流程
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-intelligence-surface/05-01-SUMMARY.md`
</output>
