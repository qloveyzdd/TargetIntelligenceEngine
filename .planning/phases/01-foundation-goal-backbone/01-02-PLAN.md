---
phase: 01-foundation-goal-backbone
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/openai.ts
  - src/features/goal-card/schema.ts
  - src/features/goal-card/normalize-goal-input.ts
  - src/features/goal-card/generate-goal-card.ts
  - src/app/api/goal-card/generate/route.ts
  - src/app/api/runs/route.ts
  - src/app/api/runs/[runId]/route.ts
  - src/components/workspace/goal-input-form.tsx
  - src/components/workspace/goal-card-editor.tsx
  - src/components/workspace/run-shell.tsx
  - src/app/page.tsx
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04]
user_setup:
  - service: openai
    why: "GoalCard 生成依赖 OpenAI Responses API"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI 控制台 API Keys 页面"
      - name: OPENAI_GOAL_MODEL
        source: ".env.local，可选覆盖，默认 gpt-5.4-mini"
must_haves:
  truths:
    - "用户输入目标描述和补充文本后可以得到结构化 GoalCard"
    - "用户可以在同页编辑 GoalCard 并显式确认保存"
    - "GoalCard 生成与确认结果会落到同一条 run 中"
  artifacts:
    - path: "src/features/goal-card/schema.ts"
      provides: "GoalCard 严格 schema"
      contains: "goalCardSchema"
    - path: "src/app/api/goal-card/generate/route.ts"
      provides: "GoalCard 生成接口"
      exports: ["POST"]
    - path: "src/components/workspace/goal-card-editor.tsx"
      provides: "GoalCard 编辑器"
      min_lines: 40
  key_links:
    - from: "src/components/workspace/goal-input-form.tsx"
      to: "/api/goal-card/generate"
      via: "fetch POST"
      pattern: "goal-card/generate"
    - from: "src/components/workspace/goal-card-editor.tsx"
      to: "/api/runs/"
      via: "PATCH 更新 run"
      pattern: "PATCH|update"
---

<objective>
实现 GoalCard 的结构化生成、编辑和确认闭环。

Purpose: 把用户自然语言输入转换为稳定的 GoalCard，并确保确认后的结果写入 run 聚合。
Output: GoalCard schema 与生成服务、生成/更新接口、输入表单与编辑器组件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md
@.planning/phases/01-foundation-goal-backbone/01-RESEARCH.md
@.planning/phases/01-foundation-goal-backbone/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 GoalCard schema 和结构化生成服务</name>
  <files>src/lib/openai.ts, src/features/goal-card/schema.ts, src/features/goal-card/normalize-goal-input.ts, src/features/goal-card/generate-goal-card.ts, src/app/api/goal-card/generate/route.ts</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-RESEARCH.md, src/features/analysis-run/types.ts, src/lib/openai.ts, src/features/goal-card/schema.ts</read_first>
  <action>在 `src/features/goal-card/schema.ts` 中声明 GoalCard JSON schema，至少包含 `name`、`category`、`jobToBeDone`、`hardConstraints`、`softPreferences`、`currentStage` 字段，并与 `src/features/analysis-run/types.ts` 中的 `GoalCard` 类型保持一致。在 `src/lib/openai.ts` 中初始化服务端 OpenAI 客户端，读取 `OPENAI_API_KEY` 和可选 `OPENAI_GOAL_MODEL`。在 `src/features/goal-card/generate-goal-card.ts` 中通过 Responses API + `json_schema` 模式生成 GoalCard，要求严格 schema 校验，禁止把自由文本原样落库。新增 `/api/goal-card/generate` 的 `POST` Route Handler，输入只接受 `inputText` 与 `inputNotes`，返回结构化 GoalCard JSON。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/goal-card/schema.ts` contains `jobToBeDone`
    - `src/features/goal-card/generate-goal-card.ts` contains `json_schema`
    - `src/app/api/goal-card/generate/route.ts` contains `inputText`
    - `src/app/api/goal-card/generate/route.ts` contains `Response.json`
  </acceptance_criteria>
  <done>GoalCard 服务边界已经固定为“文本输入 -> 严格 schema 输出”，后续 phase 可直接复用这个模式。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 run 创建、GoalCard 更新和编辑器交互</name>
  <files>src/app/api/runs/route.ts, src/app/api/runs/[runId]/route.ts, src/components/workspace/goal-input-form.tsx, src/components/workspace/goal-card-editor.tsx</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, src/features/analysis-run/repository.ts, src/app/api/runs/route.ts, src/components/workspace/goal-card-editor.tsx</read_first>
  <action>在 `/api/runs` 的 `POST` handler 中调用 `createDraftRun` 创建 `draft` run，返回 `id`、`status` 和初始输入字段。在 `/api/runs/[runId]` 中实现 `GET` 和 `PATCH`：`GET` 返回完整 run，`PATCH` 至少支持更新 `goal`、`status`、`inputNotes`。实现 `goal-input-form.tsx`，用户点击生成时先创建 draft run，再调用 `/api/goal-card/generate` 获取 GoalCard，并把结果写回当前 run。实现 `goal-card-editor.tsx`，用表单字段渲染 GoalCard，用户点击确认时调用 `PATCH /api/runs/[runId]` 保存修改后的 GoalCard，并把状态推进到 `goal_confirmed`。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/app/api/runs/route.ts` contains `createDraftRun`
    - `src/app/api/runs/[runId]/route.ts` contains `PATCH`
    - `src/components/workspace/goal-input-form.tsx` contains `/api/runs`
    - `src/components/workspace/goal-card-editor.tsx` contains `goal_confirmed`
  </acceptance_criteria>
  <done>用户已经可以从文本输入得到 GoalCard，并把编辑确认后的结果稳定写回 run。</done>
</task>

<task type="auto">
  <name>Task 3: 把 GoalCard 工作流接入首页工作台</name>
  <files>src/components/workspace/run-shell.tsx, src/app/page.tsx</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, src/components/workspace/run-shell.tsx, src/components/workspace/goal-input-form.tsx, src/components/workspace/goal-card-editor.tsx</read_first>
  <action>更新 `RunShell` 和首页，使页面上方固定为目标输入区，提交后在同页下方展示 GoalCard 编辑器。首页必须在 GoalCard 生成前显示引导态，在 GoalCard 生成后显示编辑态，在确认后保留只读或可回编状态，但 `Candidates / Evidence / Stage Goals` 区域仍只展示 Phase 1 空骨架文案。不要做多步骤向导，不要跳去新页面完成编辑。</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - `src/components/workspace/run-shell.tsx` contains `GoalCard`
    - `src/components/workspace/run-shell.tsx` contains `Candidates`
    - `src/app/page.tsx` contains `GoalInputForm`
    - 首页主流程的 Playwright 用例 exits 0
  </acceptance_criteria>
  <done>单页工作台的核心交互闭环已经成型，符合 discuss 阶段锁定的交互决策。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器输入内容 -> `/api/runs` 与 `/api/goal-card/generate`
- Next.js 服务器 -> OpenAI Responses API
- Next.js 服务器 -> PostgreSQL

## STRIDE Register
- threat_id: T-01-04
  category: tampering
  component: goal_card_generate_route
  disposition: mitigate
  mitigation_plan: "生成接口只接收 `inputText` 和 `inputNotes`，并始终通过严格 JSON schema 验证返回结果；校验失败直接返回错误，不写入数据库。"
- threat_id: T-01-05
  category: information_disclosure
  component: openai_client
  disposition: mitigate
  mitigation_plan: "OpenAI 客户端只存在于服务端文件 `src/lib/openai.ts`，客户端组件不得直接导入。"
- threat_id: T-01-06
  category: spoofing
  component: run_update_route
  disposition: mitigate
  mitigation_plan: "PATCH `/api/runs/[runId]` 必须基于 path 中的 `runId` 读取现有 run 后再更新，不接受客户端直接传完整数据库对象覆盖。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `npm run test:e2e` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 用户已经可以在单页工作台中完成 GoalCard 生成、编辑和确认
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-goal-backbone/01-02-SUMMARY.md`
</output>
