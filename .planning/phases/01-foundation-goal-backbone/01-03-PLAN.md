---
phase: 01-foundation-goal-backbone
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/features/dimensions/core-dimensions.ts
  - src/features/dimensions/build-initial-dimensions.ts
  - src/features/dimensions/build-initial-dimensions.test.ts
  - src/features/analysis-run/repository.ts
  - src/app/api/runs/[runId]/route.ts
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/analysis-placeholders.tsx
  - src/app/runs/[runId]/page.tsx
  - tests/e2e/goalcard-workflow.spec.ts
autonomous: true
requirements: [DIME-01, GOAL-04]
user_setup: []
must_haves:
  truths:
    - "GoalCard 确认后会自动生成初版通用维度列表"
    - "重新打开已确认 run 时可以看到已保存的 GoalCard 和 dimensions"
    - "Candidates、Evidence、Stage Goals 空区域会在 run 页面稳定显示"
  artifacts:
    - path: "src/features/dimensions/core-dimensions.ts"
      provides: "初版通用维度模板"
      min_lines: 30
    - path: "src/features/dimensions/build-initial-dimensions.ts"
      provides: "维度模板注入逻辑"
      contains: "buildInitialDimensions"
    - path: "src/app/runs/[runId]/page.tsx"
      provides: "run 重开页面"
      min_lines: 25
  key_links:
    - from: "src/app/api/runs/[runId]/route.ts"
      to: "src/features/dimensions/build-initial-dimensions.ts"
      via: "GoalCard 确认时生成 dimensions"
      pattern: "buildInitialDimensions"
    - from: "src/app/runs/[runId]/page.tsx"
      to: "/api/runs/"
      via: "读取 run 详情"
      pattern: "runs"
---

<objective>
把初版通用维度模板接入 GoalCard 确认流程，并完成 run 重开与空骨架展示。

Purpose: 满足 Phase 1 对 `dimensions` 骨架和 run 可重新打开的要求，让后续 phase 在固定数据容器上继续扩展。
Output: 通用维度模板、维度注入逻辑、run 详情页回显与 E2E 闭环。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md
@.planning/phases/01-foundation-goal-backbone/01-RESEARCH.md
@.planning/phases/01-foundation-goal-backbone/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义初版通用维度模板与生成函数</name>
  <files>src/features/dimensions/core-dimensions.ts, src/features/dimensions/build-initial-dimensions.ts, src/features/dimensions/build-initial-dimensions.test.ts</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, .planning/phases/01-foundation-goal-backbone/01-RESEARCH.md, src/features/analysis-run/types.ts, src/features/dimensions/core-dimensions.ts</read_first>
  <action>在 `core-dimensions.ts` 中定义 6 个固定通用维度：`cost`、`performance`、`usability`、`ecosystem`、`reliability`、`compliance`。每个维度都必须包含 `id`、`name`、`weight`、`direction`、`definition`、`evidenceNeeded`、`layer` 字段，其中 `layer` 固定为 `core`，`direction` 只能使用 `higher_better` 或 `lower_better`。在 `build-initial-dimensions.ts` 中实现 `buildInitialDimensions(goal: GoalCard)`，当前版本先直接返回这 6 个维度，并保留根据 GoalCard 调整权重的最小钩子。为该函数补 Vitest 用例，确保返回 6 条维度且权重总和接近 1。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/dimensions/core-dimensions.ts` contains `cost`
    - `src/features/dimensions/core-dimensions.ts` contains `compliance`
    - `src/features/dimensions/build-initial-dimensions.ts` contains `buildInitialDimensions`
    - `src/features/dimensions/build-initial-dimensions.test.ts` contains `weight`
  </acceptance_criteria>
  <done>Phase 1 的 `dimensions` 已经有稳定可测试的初始模板，不再依赖临时推断。</done>
</task>

<task type="auto">
  <name>Task 2: 在 GoalCard 确认时写入 dimensions 并稳定回显</name>
  <files>src/features/analysis-run/repository.ts, src/app/api/runs/[runId]/route.ts, src/components/workspace/run-shell.tsx, src/app/runs/[runId]/page.tsx</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, src/features/analysis-run/repository.ts, src/app/api/runs/[runId]/route.ts, src/features/dimensions/build-initial-dimensions.ts</read_first>
  <action>扩展 `PATCH /api/runs/[runId]` 的 GoalCard 确认分支：当请求把 run 状态推进到 `goal_confirmed` 时，如果 `dimensions` 为空，则调用 `buildInitialDimensions(goal)` 生成 6 个通用维度并一并写入 run。更新 `RunShell` 和 `src/app/runs/[runId]/page.tsx`，在 GoalCard 区域下方展示一份维度摘要列表，至少显示名称、权重和方向；重新打开已确认 run 时必须回显已保存的 GoalCard 和 dimensions，而不是重新生成。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/app/api/runs/[runId]/route.ts` contains `buildInitialDimensions`
    - `src/components/workspace/run-shell.tsx` contains `dimensions`
    - `src/app/runs/[runId]/page.tsx` contains `goal_confirmed`
    - `src/features/analysis-run/repository.ts` contains `dimensions`
  </acceptance_criteria>
  <done>GoalCard 与初版 dimensions 已经稳定绑定到同一条 run，满足 Phase 1 的骨架要求。</done>
</task>

<task type="auto">
  <name>Task 3: 固化重开流程和 Phase 1 闭环 E2E</name>
  <files>src/components/workspace/analysis-placeholders.tsx, tests/e2e/goalcard-workflow.spec.ts</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-VALIDATION.md, src/components/workspace/analysis-placeholders.tsx, tests/e2e/goalcard-workflow.spec.ts</read_first>
  <action>更新空状态组件，明确展示 `Candidates`、`Evidence`、`Stage Goals` 处于 Phase 1 placeholder 状态。编写 `tests/e2e/goalcard-workflow.spec.ts`，完整覆盖：输入目标描述与补充文本、生成 GoalCard、编辑一个字段、确认保存、自动写入 6 个通用维度、跳转或打开 `/runs/[runId]` 后看到相同 GoalCard 和维度列表。测试中不要依赖真实 OpenAI 网络请求，使用稳定 mock 或测试替身保证 E2E 可重复运行。</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - `src/components/workspace/analysis-placeholders.tsx` contains `Phase 1 placeholder`
    - `tests/e2e/goalcard-workflow.spec.ts` contains `/runs/`
    - `tests/e2e/goalcard-workflow.spec.ts` contains `GoalCard`
    - `npm run test:e2e` exits 0
  </acceptance_criteria>
  <done>Phase 1 的主闭环已经有稳定的端到端验证，不会只停留在静态骨架。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器 -> `/api/runs/[runId]`
- Next.js 服务器 -> 维度模板生成逻辑
- Next.js 服务器 -> PostgreSQL

## STRIDE Register
- threat_id: T-01-07
  category: tampering
  component: dimensions_injection
  disposition: mitigate
  mitigation_plan: "只有服务端在 `goal_confirmed` 状态迁移时才能生成初版 dimensions；客户端不能直接提交自定义 JSONB 覆盖。"
- threat_id: T-01-08
  category: spoofing
  component: run_detail_lookup
  disposition: mitigate
  mitigation_plan: "详情页和读取接口都基于 path 中的 `runId` 查询真实记录；不存在时返回明确空态，不回退到任意默认 run。"
- threat_id: T-01-09
  category: denial_of_service
  component: e2e_and_placeholder_flow
  disposition: mitigate
  mitigation_plan: "E2E 使用稳定 mock 而不依赖真实 OpenAI 网络调用，避免测试阶段因外部服务抖动阻塞交付。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `npm run test:e2e` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- run 重开、GoalCard 回显、初版 dimensions 注入和空骨架展示全部闭环
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-goal-backbone/01-03-SUMMARY.md`
</output>
