---
phase: 01-foundation-goal-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - docker-compose.yml
  - .env.example
  - drizzle.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/runs/[runId]/page.tsx
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/analysis-placeholders.tsx
  - src/db/client.ts
  - src/db/schema.ts
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.ts
autonomous: true
requirements: [GOAL-01, GOAL-02]
user_setup: []
must_haves:
  truths:
    - "项目可以作为单个 Next.js 应用启动，并显示 Phase 1 工作台骨架"
    - "服务端可以创建、读取一条 analysis run 聚合记录"
    - "analysis run 聚合包含 goal、dimensions、candidates、evidence、stageGoals 五类字段骨架"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "analysis_runs 主表与 JSONB 骨架"
      contains: "analysis_runs"
    - path: "src/features/analysis-run/repository.ts"
      provides: "run 聚合读写仓储"
      min_lines: 40
    - path: "src/app/page.tsx"
      provides: "首页工作台入口"
      min_lines: 20
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/workspace/run-shell.tsx"
      via: "首页挂载工作台壳层"
      pattern: "run-shell|RunShell"
    - from: "src/features/analysis-run/repository.ts"
      to: "src/db/schema.ts"
      via: "Drizzle schema access"
      pattern: "analysisRuns|analysis_runs"
---

<objective>
建立 Phase 1 的应用骨架、数据库骨架和 `analysis run` 聚合模型。

Purpose: 为后续 GoalCard 生成、编辑和维度注入提供稳定的应用入口与持久化容器。
Output: 可启动的 Next.js 单应用、PostgreSQL/Drizzle 基础配置、run 聚合 schema、首页与 run 详情页空壳。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md
@.planning/phases/01-foundation-goal-backbone/01-RESEARCH.md
@.planning/phases/01-foundation-goal-backbone/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立单应用脚手架和本地开发基础设施</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, docker-compose.yml, .env.example, drizzle.config.ts, src/app/layout.tsx</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, .planning/phases/01-foundation-goal-backbone/01-RESEARCH.md, package.json, .env.example</read_first>
  <action>创建一个 Next.js App Router 项目骨架，`package.json` 必须包含 `dev`、`build`、`lint`、`typecheck`、`test:unit`、`test:e2e`、`check` 脚本；依赖包含 `next`、`react`、`react-dom`、`drizzle-orm`、`pg`、`openai`；开发依赖包含 `typescript`、`drizzle-kit`、`vitest`、`playwright`、`eslint`。新增 `docker-compose.yml` 提供单个 `postgres` 服务，默认映射 `5432:5432`，数据库名固定为 `target_intelligence_engine`。新增 `.env.example`，至少包含 `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/target_intelligence_engine`、`OPENAI_API_KEY=`、`OPENAI_GOAL_MODEL=gpt-5.4-mini`。`src/app/layout.tsx` 提供最小 HTML 壳层和中文页面标题。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `package.json` contains `"check": "npm run lint && npm run typecheck && npm run test:unit -- --run"`
    - `docker-compose.yml` contains `target_intelligence_engine`
    - `.env.example` contains `OPENAI_GOAL_MODEL=gpt-5.4-mini`
    - `src/app/layout.tsx` contains `Target Intelligence Engine`
  </acceptance_criteria>
  <done>脚手架、开发脚本、本地数据库配置和基础 layout 全部存在且可被类型检查加载。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 analysis run 聚合 schema、类型和仓储</name>
  <files>src/db/client.ts, src/db/schema.ts, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.ts</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, .planning/phases/01-foundation-goal-backbone/01-RESEARCH.md, src/db/schema.ts, src/features/analysis-run/types.ts</read_first>
  <action>在 `src/features/analysis-run/types.ts` 中定义 Phase 1 所需核心对象类型：`GoalCard`、`Dimension`、`Candidate`、`Evidence`、`StageGoal`、`AnalysisRun`。在 `src/db/schema.ts` 中声明 `analysis_runs` 表，字段必须包含 `id`、`status`、`inputText`、`inputNotes`、`goal`、`dimensions`、`candidates`、`evidence`、`stageGoals`、`createdAt`、`updatedAt`，其中后五类对象按 JSONB 存储，数组字段默认 `[]`。在 `src/features/analysis-run/repository.ts` 中提供 `createDraftRun`、`getRunById`、`updateRunAggregate` 三个服务函数，全部只通过服务端数据库访问，不允许在客户端文件里直接导入数据库客户端。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `analysisRuns`
    - `src/db/schema.ts` contains `dimensions` and `stageGoals`
    - `src/features/analysis-run/repository.ts` contains `createDraftRun`
    - `src/features/analysis-run/repository.ts` contains `updateRunAggregate`
  </acceptance_criteria>
  <done>run 聚合模型已经稳定成型，后续计划可以直接围绕这组类型和仓储扩展。</done>
</task>

<task type="auto">
  <name>Task 3: 建立首页与 run 详情页的空骨架</name>
  <files>src/app/page.tsx, src/app/runs/[runId]/page.tsx, src/components/workspace/run-shell.tsx, src/components/workspace/analysis-placeholders.tsx</files>
  <read_first>.planning/phases/01-foundation-goal-backbone/01-CONTEXT.md, .planning/phases/01-foundation-goal-backbone/01-RESEARCH.md, src/app/page.tsx, src/components/workspace/run-shell.tsx</read_first>
  <action>实现首页 `src/app/page.tsx`，渲染 `RunShell` 组件作为 Phase 1 工作台壳层。`RunShell` 必须明确分成顶部输入区域和下方结果区域，并预留 `GoalCard`、`Candidates`、`Evidence`、`Stage Goals` 四块位置。实现 `src/app/runs/[runId]/page.tsx`，能够按 `runId` 调用仓储读取 run 并展示相同骨架；当 run 不存在时，返回明确的空态提示。`analysis-placeholders.tsx` 统一渲染 `Candidates / Evidence / Stage Goals` 的空状态文案，避免在 Phase 1 提前实现真实业务逻辑。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contains `RunShell`
    - `src/app/runs/[runId]/page.tsx` contains `runId`
    - `src/components/workspace/analysis-placeholders.tsx` contains `Candidates`
    - `src/components/workspace/analysis-placeholders.tsx` contains `Stage Goals`
  </acceptance_criteria>
  <done>首页和 run 详情页都能展示统一的分析骨架，且未实现区域明确呈现为空状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器表单输入 -> Next.js Route Handlers
- Next.js 服务器 -> PostgreSQL
- Next.js 服务器 -> OpenAI API（后续 plan 接入）

## STRIDE Register
- threat_id: T-01-01
  category: information_disclosure
  component: app_shell_and_env
  disposition: mitigate
  mitigation_plan: "所有数据库和 OpenAI 配置只放在服务端环境变量中；客户端组件不得导入 `src/db/client.ts` 或 `src/lib/openai.ts`。"
- threat_id: T-01-02
  category: tampering
  component: analysis_run_repository
  disposition: mitigate
  mitigation_plan: "所有 run 写入都通过 `updateRunAggregate` 等服务函数统一执行，禁止客户端直写数据库。"
- threat_id: T-01-03
  category: denial_of_service
  component: run_detail_page
  disposition: mitigate
  mitigation_plan: "详情页对缺失 run id 返回明确空态，不做无限重试或循环加载。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] 首页和 run 详情页都能被 Next 编译加载
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 项目已经具备单应用脚手架、run 聚合 schema 和统一工作台空骨架
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-goal-backbone/01-01-SUMMARY.md`
</output>
