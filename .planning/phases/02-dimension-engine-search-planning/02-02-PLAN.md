---
phase: 02-dimension-engine-search-planning
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/db/schema.ts
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.ts
  - src/features/analysis-run/repository.test.ts
  - src/features/search-plan/schema.ts
  - src/features/search-plan/schema.test.ts
  - src/features/search-plan/build-search-plan-input.ts
  - src/features/search-plan/generate-search-plan.ts
  - src/features/search-plan/generate-search-plan.test.ts
  - src/app/api/runs/[runId]/search-plan/route.ts
autonomous: true
requirements: [SRCH-01, SRCH-02]
user_setup: []
must_haves:
  truths:
    - "系统可以基于已确认的 GoalCard 和 enabled dimensions 同时生成 same_goal 与 dimension_leader 两类 SearchPlan 草案"
    - "SearchPlan 草案是独立工件，确认后写回当前 run，而不是冒充 candidates 结果"
    - "所有 enabled 维度都允许生成 dimension_leader 计划项"
  artifacts:
    - path: "src/features/search-plan/generate-search-plan.ts"
      provides: "SearchPlan 草案生成服务"
      contains: "generateSearchPlan"
    - path: "src/features/search-plan/schema.ts"
      provides: "SearchPlan 结构和服务端收敛规则"
      contains: "searchPlanSchema"
    - path: "src/app/api/runs/[runId]/search-plan/route.ts"
      provides: "SearchPlan 草案生成与确认接口"
      exports: ["POST", "PATCH"]
  key_links:
    - from: "src/app/api/runs/[runId]/search-plan/route.ts"
      to: "src/features/search-plan/build-search-plan-input.ts"
      via: "构造 SearchPlan 输入快照"
      pattern: "buildSearchPlanInput"
    - from: "src/app/api/runs/[runId]/search-plan/route.ts"
      to: "src/features/search-plan/generate-search-plan.ts"
      via: "POST 生成 SearchPlan 草案"
      pattern: "generateSearchPlan"
---

<objective>
实现双模式 SearchPlan 草案服务与 run 级确认链路。
Purpose: 把“该怎么搜”从隐式提示词变成显式、可确认、可重开的结构化工件，为下一阶段候选召回提供稳定输入。
Output: SearchPlan 类型与 schema、same_goal + dimension_leader 草案生成服务、SearchPlan Route Handler、run 状态推进到 `search_plan_confirmed`。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md
@.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md
@.planning/phases/02-dimension-engine-search-planning/02-VALIDATION.md
@.planning/phases/02-dimension-engine-search-planning/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 run 聚合以持久化 SearchPlan 工件和状态</name>
  <files>src/db/schema.ts, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.ts, src/features/analysis-run/repository.test.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md, src/db/schema.ts, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts</read_first>
  <action>在 `src/features/analysis-run/types.ts` 中新增 `SearchPlanMode`、`SearchPlanItem`、`SearchPlan` 类型，并把 `AnalysisRunStatus` 扩展为 `search_plan_ready`、`search_plan_confirmed`。在 `AnalysisRun` 中新增 `searchPlan: SearchPlan | null`。在 `src/db/schema.ts` 为 `analysis_runs` 增加 `search_plan` JSONB 列，类型映射到 `SearchPlan | null`。更新 `run-mappers.ts`，只接受包含 `status`、`items`、`confirmedAt` 的合法 SearchPlan；每个 item 必须具备 `mode`、`dimensionId`、`query`、`whatToFind`、`whyThisSearch`、`expectedCandidateCount`、`sourceHints`。更新 `repository.test.ts`，确保 SearchPlan 写入后可以完整读回，且状态可以推进到 `search_plan_confirmed`。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/analysis-run/types.ts` contains `export type SearchPlan`
    - `src/features/analysis-run/types.ts` contains `search_plan_confirmed`
    - `src/db/schema.ts` contains `searchPlan`
    - `src/features/analysis-run/run-mappers.ts` contains `expectedCandidateCount`
    - `src/features/analysis-run/repository.test.ts` contains `search_plan_confirmed`
  </acceptance_criteria>
  <done>run 聚合已经具备 SearchPlan 存储能力，Phase 3 可以直接消费这个工件。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 same_goal 与 dimension_leader 双模式 SearchPlan 生成服务</name>
  <files>src/features/search-plan/schema.ts, src/features/search-plan/schema.test.ts, src/features/search-plan/build-search-plan-input.ts, src/features/search-plan/generate-search-plan.ts, src/features/search-plan/generate-search-plan.test.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md, src/features/analysis-run/types.ts, src/features/dimensions/normalize-dimension-weights.ts, src/features/goal-card/generate-goal-card.ts</read_first>
  <action>新增 `schema.ts`，定义严格的 SearchPlan JSON schema，固定 `mode` 只能为 `same_goal` 或 `dimension_leader`。新增 `build-search-plan-input.ts`，输入必须是已确认的 `GoalCard` 与 `enabled=true` 的维度数组；其中 `same_goal` 部分限制为 1 到 3 个草案项，`dimension_leader` 部分要求对每个 enabled 维度都生成 1 个草案项。新增 `generate-search-plan.ts`，复用现有 OpenAI 客户端和 `Responses API + json_schema`；在 `MOCK_OPENAI=true` 时返回稳定草案，至少包含 1 条 `same_goal` 与全部 enabled 维度的 `dimension_leader` 项。每个 SearchPlan item 必须写明 `whatToFind`、`whyThisSearch`、`expectedCandidateCount` 和 `sourceHints`，其中 `sourceHints` 至少使用 `official_site`、`docs`、`pricing`、`review` 这些明确字符串之一。补齐 Vitest 测试，覆盖两种模式都存在、disabled 维度不会产出 leader 计划、草案项说明字段不为空。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/search-plan/schema.ts` contains `same_goal`
    - `src/features/search-plan/schema.ts` contains `dimension_leader`
    - `src/features/search-plan/build-search-plan-input.ts` contains `enabled`
    - `src/features/search-plan/generate-search-plan.ts` contains `json_schema`
    - `src/features/search-plan/generate-search-plan.test.ts` contains `disabled`
    - `src/features/search-plan/generate-search-plan.test.ts` contains `expectedCandidateCount`
  </acceptance_criteria>
  <done>SearchPlan 生成逻辑已经被收敛成固定结构，不再依赖隐式 prompt 约定。</done>
</task>

<task type="auto">
  <name>Task 3: 提供 SearchPlan 草案生成与确认接口</name>
  <files>src/app/api/runs/[runId]/search-plan/route.ts, src/features/analysis-run/repository.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md, src/app/api/runs/[runId]/dimensions/route.ts, src/features/search-plan/build-search-plan-input.ts, src/features/search-plan/generate-search-plan.ts</read_first>
  <action>新增 `src/app/api/runs/[runId]/search-plan/route.ts`。`POST` 行为固定为: 读取 run，要求 `goal` 已存在且状态至少为 `dimensions_ready`，并且至少有一个 enabled 维度；如果 `searchPlan` 已存在且未传 `forceRegenerate=true`，直接返回现有草案；否则用 `buildSearchPlanInput()` 组装输入，调用 `generateSearchPlan()`，把结果以 `{ status: "draft", confirmedAt: null }` 写入 `run.searchPlan`，并把 run 状态推进到 `search_plan_ready`。`PATCH` 行为固定为: 只接受合法 SearchPlan payload，把 `confirmedAt` 设置为当前 ISO 时间，状态写成 `confirmed`，同时把 run 状态推进到 `search_plan_confirmed`。任何缺 GoalCard、缺 enabled dimensions、缺合法 SearchPlan 的场景都必须明确返回 400。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/app/api/runs/[runId]/search-plan/route.ts` contains `search_plan_ready`
    - `src/app/api/runs/[runId]/search-plan/route.ts` contains `search_plan_confirmed`
    - `src/app/api/runs/[runId]/search-plan/route.ts` contains `forceRegenerate`
    - `src/app/api/runs/[runId]/search-plan/route.ts` contains `buildSearchPlanInput`
    - `src/app/api/runs/[runId]/search-plan/route.ts` contains `generateSearchPlan`
  </acceptance_criteria>
  <done>SearchPlan 草案已经有完整的服务端生成和确认边界，下一步只需要把它接入工作台。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器 SearchPlan 请求 -> `/api/runs/[runId]/search-plan`
- Next.js 服务端 -> OpenAI Responses API
- Next.js 服务端 -> PostgreSQL run 聚合

## STRIDE Register
- threat_id: T-02-04
  category: tampering
  component: search_plan_payload
  disposition: mitigate
  mitigation_plan: "SearchPlan 草案和确认 payload 都必须经过 `search-plan/schema.ts` 收敛，禁止客户端直接伪造缺字段或未知 mode 的计划项。"
- threat_id: T-02-05
  category: spoofing
  component: leader_plan_scope
  disposition: mitigate
  mitigation_plan: "SearchPlan 输入快照只允许读取 `enabled=true` 的维度，disabled 维度绝不生成 `dimension_leader` 项。"
- threat_id: T-02-06
  category: denial_of_service
  component: search_plan_regeneration
  disposition: mitigate
  mitigation_plan: "若 run 已有 SearchPlan 草案且未显式 `forceRegenerate`，POST 接口直接返回现有结果，避免工作台重复刷新时反复调用模型。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] SearchPlan 生成接口对无 enabled 维度场景返回 400
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- run 已经能稳定保存 `same_goal` 和 `dimension_leader` 双模式 SearchPlan 草案与确认结果
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dimension-engine-search-planning/02-02-SUMMARY.md`
</output>
