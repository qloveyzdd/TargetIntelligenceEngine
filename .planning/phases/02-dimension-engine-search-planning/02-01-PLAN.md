---
phase: 02-dimension-engine-search-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis-run/types.ts
  - src/features/analysis-run/run-mappers.ts
  - src/features/analysis-run/repository.ts
  - src/features/analysis-run/repository.test.ts
  - src/features/dimensions/dimension-schema.ts
  - src/features/dimensions/dimension-schema.test.ts
  - src/features/dimensions/generate-dynamic-dimensions.ts
  - src/features/dimensions/merge-dimensions.ts
  - src/features/dimensions/merge-dimensions.test.ts
  - src/features/dimensions/normalize-dimension-weights.ts
  - src/features/dimensions/normalize-dimension-weights.test.ts
  - src/app/api/runs/[runId]/dimensions/route.ts
autonomous: true
requirements: [DIME-02, DIME-03, DIME-04]
user_setup: []
must_haves:
  truths:
    - "系统可以在 core dimensions 基础上生成并合并 domain / project 维度草案"
    - "用户可以修改 weight、direction、definition、evidenceNeeded，并启用或禁用维度"
    - "维度草案保存在同一条 analysis run 中，重开 run 时不会被重新发明"
  artifacts:
    - path: "src/features/dimensions/generate-dynamic-dimensions.ts"
      provides: "domain / project 维度草案生成服务"
      contains: "generateDynamicDimensions"
    - path: "src/features/dimensions/merge-dimensions.ts"
      provides: "三层维度去重与合并逻辑"
      contains: "mergeDimensions"
    - path: "src/app/api/runs/[runId]/dimensions/route.ts"
      provides: "维度草案生成与确认接口"
      exports: ["POST", "PATCH"]
  key_links:
    - from: "src/app/api/runs/[runId]/dimensions/route.ts"
      to: "src/features/dimensions/generate-dynamic-dimensions.ts"
      via: "POST 生成维度草案"
      pattern: "generateDynamicDimensions"
    - from: "src/app/api/runs/[runId]/dimensions/route.ts"
      to: "src/features/dimensions/normalize-dimension-weights.ts"
      via: "PATCH 保存编辑后的维度"
      pattern: "normalizeDimensionWeights"
---

<objective>
实现 Phase 2 的三层维度引擎、维度可编辑数据契约和 run 级维度确认链路。
Purpose: 在 Phase 1 已有 core dimensions 的基础上，补齐 domain / project 层生成与统一持久化，让维度从“临时显示”变成“可编辑、可确认、可重开”的对象。
Output: 动态维度 schema 与生成服务、三层维度合并与权重归一化逻辑、维度专用 Route Handler、run 状态推进到 `dimensions_ready`。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md
@.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md
@.planning/phases/02-dimension-engine-search-planning/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 run 聚合与维度结构，支持维度编辑生命周期</name>
  <files>src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.ts, src/features/analysis-run/repository.test.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md, src/features/analysis-run/types.ts, src/features/analysis-run/run-mappers.ts, src/features/analysis-run/repository.ts</read_first>
  <action>在 `src/features/analysis-run/types.ts` 里把 `Dimension` 扩展为包含 `enabled: boolean`，并把 `AnalysisRunStatus` 明确扩展为 `draft`、`goal_ready`、`goal_confirmed`、`dimensions_ready`。保持 `layer` 仍只允许 `core`、`domain`、`project`。在 `run-mappers.ts` 中更新 `toDimensions()`，要求只有同时具备 `id`、`name`、`weight`、`direction`、`definition`、`evidenceNeeded`、`layer`、`enabled` 的对象才能进入运行时数据。更新 `repository.test.ts`，增加一条带 `enabled` 维度的更新用例，确保持久化和读回后 `enabled`、`layer`、`weight` 都不丢失。不要在这个任务里引入 SearchPlan 结构，先把维度生命周期收紧。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/analysis-run/types.ts` contains `enabled: boolean`
    - `src/features/analysis-run/types.ts` contains `dimensions_ready`
    - `src/features/analysis-run/run-mappers.ts` contains `typeof dimension.enabled === "boolean"`
    - `src/features/analysis-run/repository.test.ts` contains `dimensions_ready`
  </acceptance_criteria>
  <done>run 聚合已经能稳定承载可编辑维度，后续 UI 和 SearchPlan 都能直接依赖这组结构。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 domain / project 维度草案生成、三层合并和权重归一化</name>
  <files>src/features/dimensions/dimension-schema.ts, src/features/dimensions/dimension-schema.test.ts, src/features/dimensions/generate-dynamic-dimensions.ts, src/features/dimensions/merge-dimensions.ts, src/features/dimensions/merge-dimensions.test.ts, src/features/dimensions/normalize-dimension-weights.ts, src/features/dimensions/normalize-dimension-weights.test.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md, src/features/dimensions/core-dimensions.ts, src/features/dimensions/build-initial-dimensions.ts, src/features/goal-card/generate-goal-card.ts</read_first>
  <action>新增 `dimension-schema.ts`，显式定义动态维度草案 schema，只允许模型返回 `domain` 或 `project` 两层，且每条维度都必须包含 `id`、`name`、`weight`、`direction`、`definition`、`evidenceNeeded`、`layer`、`enabled`。新增 `generate-dynamic-dimensions.ts`，复用现有 OpenAI 客户端和 `Responses API + json_schema`，输入为 `GoalCard` 与现有 core dimensions；在 `MOCK_OPENAI=true` 时固定返回稳定的 domain / project 维度，避免测试依赖网络。新增 `merge-dimensions.ts`，以已有 core dimensions 为底座，按标准化 `id` 优先、`name` 兜底去重，禁止 domain / project 覆盖 core。新增 `normalize-dimension-weights.ts`，只对 `enabled=true` 的维度重新归一化到 1，disabled 维度保留但不参与归一化。补齐对应 Vitest 测试，至少覆盖: 核心维度不被覆盖、重复动态维度被合并、enabled 权重总和接近 1、disabled 维度不会参与总和。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/features/dimensions/dimension-schema.ts` contains `dynamicDimensionSchema`
    - `src/features/dimensions/generate-dynamic-dimensions.ts` contains `json_schema`
    - `src/features/dimensions/merge-dimensions.ts` contains `mergeDimensions`
    - `src/features/dimensions/normalize-dimension-weights.ts` contains `enabled`
    - `src/features/dimensions/merge-dimensions.test.ts` contains `core`
    - `src/features/dimensions/normalize-dimension-weights.test.ts` contains `toBeCloseTo(1`
  </acceptance_criteria>
  <done>三层维度引擎已经从“只有 core 模板”升级为“可生成、可合并、可测试”的稳定服务。</done>
</task>

<task type="auto">
  <name>Task 3: 提供维度草案生成与确认接口，推进 run 到 dimensions_ready</name>
  <files>src/app/api/runs/[runId]/dimensions/route.ts, src/features/analysis-run/repository.ts</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md, src/app/api/runs/[runId]/route.ts, src/features/analysis-run/repository.ts, src/features/dimensions/generate-dynamic-dimensions.ts, src/features/dimensions/merge-dimensions.ts</read_first>
  <action>新增 `src/app/api/runs/[runId]/dimensions/route.ts`。`POST` 行为固定为: 读取现有 run；要求 `goal` 已存在且状态至少为 `goal_confirmed`；若当前 run 已经包含非 core 维度且没有 `forceRegenerate=true`，则直接返回现有维度；否则以当前 `run.dimensions` 中的 core 维度作为底座，调用 `generateDynamicDimensions()`，再用 `mergeDimensions()` 与 `normalizeDimensionWeights()` 生成完整草案并写回 run。`PATCH` 行为固定为: 接收完整维度数组，先用服务端 schema 收敛，再归一化 enabled 维度权重，最后写回 run 并把状态推进到 `dimensions_ready`。错误分支必须明确返回 400/404，不允许静默吞错。</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `src/app/api/runs/[runId]/dimensions/route.ts` contains `forceRegenerate`
    - `src/app/api/runs/[runId]/dimensions/route.ts` contains `dimensions_ready`
    - `src/app/api/runs/[runId]/dimensions/route.ts` contains `normalizeDimensionWeights`
    - `src/app/api/runs/[runId]/dimensions/route.ts` contains `Response.json`
  </acceptance_criteria>
  <done>维度草案已经有明确的服务端边界，执行阶段只需要在 UI 上调用这条接口即可。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器维度编辑请求 -> `/api/runs/[runId]/dimensions`
- Next.js 服务端 -> OpenAI Responses API
- Next.js 服务端 -> PostgreSQL run 聚合

## STRIDE Register
- threat_id: T-02-01
  category: tampering
  component: dimension_patch_payload
  disposition: mitigate
  mitigation_plan: "维度数组必须先经过 `dimension-schema.ts` 的服务端收敛，再写入 run；不允许客户端任意注入未知字段或非法 direction。"
- threat_id: T-02-02
  category: spoofing
  component: dimension_generation_scope
  disposition: mitigate
  mitigation_plan: "动态维度生成只允许产出 `domain` 和 `project`，且 `mergeDimensions()` 明确禁止覆盖 `core` 维度。"
- threat_id: T-02-03
  category: denial_of_service
  component: regenerate_dimensions
  disposition: mitigate
  mitigation_plan: "POST `/dimensions` 在已有非 core 维度且未显式 `forceRegenerate` 时直接返回现有结果，避免页面刷新时反复触发模型调用。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] 维度服务端接口对非法 payload 返回 400
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- run 已经能保存和确认三层维度草案，且符合 `DIME-02`、`DIME-03`、`DIME-04`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dimension-engine-search-planning/02-01-SUMMARY.md`
</output>
