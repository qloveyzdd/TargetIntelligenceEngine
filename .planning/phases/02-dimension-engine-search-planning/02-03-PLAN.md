---
phase: 02-dimension-engine-search-planning
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/workspace/run-shell.tsx
  - src/components/workspace/dimension-editor.tsx
  - src/components/workspace/search-plan-panel.tsx
  - src/components/workspace/analysis-placeholders.tsx
  - src/components/workspace/goal-card-editor.tsx
  - tests/e2e/dimension-search-plan-workflow.spec.ts
autonomous: true
requirements: [DIME-02, DIME-03, DIME-04, SRCH-01, SRCH-02]
user_setup: []
must_haves:
  truths:
    - "用户可以在同一工作台里生成、编辑并确认维度草案"
    - "用户可以在同一工作台里查看、确认 SearchPlan 草案，且草案按 same_goal 与 dimension_leader 分组展示"
    - "重开 run 后，已确认的维度与 SearchPlan 会稳定回显"
  artifacts:
    - path: "src/components/workspace/dimension-editor.tsx"
      provides: "维度编辑器"
      min_lines: 80
    - path: "src/components/workspace/search-plan-panel.tsx"
      provides: "SearchPlan 草案面板"
      min_lines: 80
    - path: "tests/e2e/dimension-search-plan-workflow.spec.ts"
      provides: "Phase 2 主链路 E2E"
      contains: "dimension"
  key_links:
    - from: "src/components/workspace/dimension-editor.tsx"
      to: "/api/runs/"
      via: "维度草案生成与保存"
      pattern: "dimensions"
    - from: "src/components/workspace/search-plan-panel.tsx"
      to: "/api/runs/"
      via: "SearchPlan 草案生成与确认"
      pattern: "search-plan"
---

<objective>
把三层维度引擎与 SearchPlan 草案接入现有单页工作台，并补齐 Phase 2 的端到端验证。
Purpose: 让 Phase 2 真正成为用户可操作的闭环，而不是只有后端工件。
Output: 维度编辑器、SearchPlan 草案面板、工作台状态编排、重开 run 回显、稳定可跑的 Phase 2 E2E。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md
@.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md
@.planning/phases/02-dimension-engine-search-planning/02-VALIDATION.md
@.planning/phases/02-dimension-engine-search-planning/02-01-PLAN.md
@.planning/phases/02-dimension-engine-search-planning/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在工作台接入维度草案生成、编辑与确认</name>
  <files>src/components/workspace/run-shell.tsx, src/components/workspace/dimension-editor.tsx, src/components/workspace/goal-card-editor.tsx</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-CONTEXT.md, src/components/workspace/run-shell.tsx, src/components/workspace/goal-card-editor.tsx, src/components/workspace/goal-input-form.tsx</read_first>
  <action>新增 `src/components/workspace/dimension-editor.tsx`，继续沿用当前 `useTransition + fetch` 模式，不引入表单库。编辑器必须支持: 1) 在 `goal_confirmed` 后点击“Generate dimension draft” 调用 `POST /api/runs/[runId]/dimensions`；2) 对每条维度编辑 `weight`、`direction`、`definition`、`evidenceNeeded`；3) 通过 checkbox 或 switch 控制 `enabled`；4) 点击保存时调用 `PATCH /api/runs/[runId]/dimensions`。更新 `run-shell.tsx`，将当前只读 `DimensionSummary` 替换为阶段感知的维度区域：未生成时显示引导文案，已生成时显示编辑器，`dimensions_ready` 后显示已保存状态和可再次编辑入口。不要把维度编辑拆到新页面，保持单页工作台。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/components/workspace/dimension-editor.tsx` contains `Generate dimension draft`
    - `src/components/workspace/dimension-editor.tsx` contains `enabled`
    - `src/components/workspace/dimension-editor.tsx` contains `evidenceNeeded`
    - `src/components/workspace/run-shell.tsx` contains `dimensions_ready`
  </acceptance_criteria>
  <done>用户已经能在当前工作台真正编辑和确认维度，而不是只看只读摘要。</done>
</task>

<task type="auto">
  <name>Task 2: 在工作台接入 SearchPlan 草案展示与确认</name>
  <files>src/components/workspace/run-shell.tsx, src/components/workspace/search-plan-panel.tsx, src/components/workspace/analysis-placeholders.tsx</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-RESEARCH.md, src/components/workspace/run-shell.tsx, src/components/workspace/analysis-placeholders.tsx</read_first>
  <action>新增 `src/components/workspace/search-plan-panel.tsx`。面板必须在 `dimensions_ready` 后可用，并支持: 1) 点击“Generate search plan” 调用 `POST /api/runs/[runId]/search-plan`；2) 把草案项按 `same_goal` 与 `dimension_leader` 两组展示；3) 对每个计划项明确显示 `whatToFind`、`whyThisSearch`、`expectedCandidateCount`、`sourceHints`；4) 点击确认时调用 `PATCH /api/runs/[runId]/search-plan` 并把 run 状态推进到 `search_plan_confirmed`。更新 `run-shell.tsx`，把 SearchPlan 面板插在 Candidates placeholder 之前，让后面的 `Candidates / Evidence / Stage Goals` 仍保持 Phase 2 前的 placeholder 语义。更新 `analysis-placeholders.tsx` 文案，明确说明候选、证据和阶段目标将在后续 phase 解锁，避免与 SearchPlan 混淆。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/components/workspace/search-plan-panel.tsx` contains `same_goal`
    - `src/components/workspace/search-plan-panel.tsx` contains `dimension_leader`
    - `src/components/workspace/search-plan-panel.tsx` contains `whyThisSearch`
    - `src/components/workspace/run-shell.tsx` contains `search_plan_confirmed`
    - `src/components/workspace/analysis-placeholders.tsx` contains `SearchPlan`
  </acceptance_criteria>
  <done>SearchPlan 已经从后端工件变成工作台中的可见草案与确认步骤。</done>
</task>

<task type="auto">
  <name>Task 3: 补齐 Phase 2 主链路 E2E，验证重开 run 后的稳定回显</name>
  <files>tests/e2e/dimension-search-plan-workflow.spec.ts, src/components/workspace/run-shell.tsx</files>
  <read_first>.planning/phases/02-dimension-engine-search-planning/02-VALIDATION.md, tests/e2e/goalcard-workflow.spec.ts, src/components/workspace/run-shell.tsx</read_first>
  <action>新增 `tests/e2e/dimension-search-plan-workflow.spec.ts`。测试必须完整覆盖: 输入目标描述与补充文本、生成并确认 GoalCard、生成维度草案、禁用至少一个维度、修改至少一个维度 definition 与 evidenceNeeded、保存并确认维度、生成 SearchPlan 草案、看到 `same_goal` 和 `dimension_leader` 两组、确认 SearchPlan、打开 `/runs/[runId]` 后仍能看到相同的 enabled/disabled 状态和 SearchPlan 草案内容。整个 E2E 继续使用稳定 mock，不允许依赖真实 OpenAI 或真实外部搜索。执行完后应保证 `npm run test:e2e` 在干净 checkout 下可重复通过。</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - `tests/e2e/dimension-search-plan-workflow.spec.ts` contains `same_goal`
    - `tests/e2e/dimension-search-plan-workflow.spec.ts` contains `dimension_leader`
    - `tests/e2e/dimension-search-plan-workflow.spec.ts` contains `/runs/`
    - `tests/e2e/dimension-search-plan-workflow.spec.ts` contains `enabled`
    - `npm run test:e2e` exits 0
  </acceptance_criteria>
  <done>Phase 2 的用户主链路已经被端到端锁住，后续进入 Phase 3 时不会把“计划草案”这层弄丢。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- 浏览器工作台交互 -> `/api/runs/[runId]/dimensions` 与 `/api/runs/[runId]/search-plan`
- Next.js 客户端状态 -> 服务端持久化 run
- Playwright E2E -> mock OpenAI 输出

## STRIDE Register
- threat_id: T-02-07
  category: tampering
  component: workspace_edit_flow
  disposition: mitigate
  mitigation_plan: "工作台所有保存与确认动作都必须走服务端接口，客户端本地状态不能被当作最终真相。"
- threat_id: T-02-08
  category: information_disclosure
  component: search_plan_display
  disposition: mitigate
  mitigation_plan: "SearchPlan 面板只展示草案字段，不展示真实外部抓取内容，也不暴露任何服务端密钥或原始 prompt。"
- threat_id: T-02-09
  category: denial_of_service
  component: phase_2_e2e
  disposition: mitigate
  mitigation_plan: "E2E 使用稳定 mock 输出而不是网络请求，避免自动化验证被外部服务抖动卡住。"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `npm run test:e2e` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 用户可以在单页工作台里完成维度确认和 SearchPlan 确认，并在重开 run 后看到一致结果
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dimension-engine-search-planning/02-03-SUMMARY.md`
</output>
